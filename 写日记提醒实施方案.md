# 写日记提醒 · 实施方案（修订版，待实施）

> 目标：在「我的 → 设置抽屉」里加一个开关 + 可设置的提醒时间，到点发本地通知。
> 只做这一条链路。不做「当天已写日记就跳过」。
>
> 历史背景：该功能此前实现过两次、删除过两次（`b3c04a7`/`79432de` 引入，`c9e6199`/`58fad5c` 删除），
> 用户的描述是"总是失效"。本方案的核心是消除导致失效的设计问题，而不是重写一遍同名功能。

## 一、已确认的决策

| 项 | 决定 |
| --- | --- |
| 通知点击落点 | `rootNavigatorKey` push 独立 `DiaryScreen` 路由（不切 Tab），沿用 `HomeWidgetActionRouter` 范式 |
| 调度强度 | `AndroidScheduleMode.exactAllowWhileIdle` |
| 默认时间 | 21:00 |
| 已写日记是否跳过 | 否，到点一律提醒 |
| 身份隔离 | 跟随身份（`identity_g_*` / `identity_j_*`），未选身份时开关禁用 |

## 二、历史实现的风险与本次硬约束

下面是从 Git 中旧实现读出的静态风险，不是已由设备日志证明的故障归因。删除提交 `c9e6199` / `58fad5c` 没有留下具体失败日志。

从被删代码能确认这些设计问题：

1. **应用自己维护的一次性续订链**：旧 `diary_notification_service.dart` 的
   `_registerNextReminder` 只注册下一次，再靠 Dart 到点回调注册明天；这段应用代码和后台
   isolate 任何一步失败都可能断链。新方案移除这段自建链。注意，`flutter_local_notifications`
   的 Android 实现仍由原生 receiver 在每次触发后登记下一次；这是插件管理的原生续订，必须纳入日志和真机验证。
2. **后台 isolate 反射挂插件**：旧 `AlarmPluginRegistrar.kt` 用 `Class.forName` +
   `getDeclaredField("flutterBackgroundExecutor")` / `("backgroundFlutterEngine")` 读
   `android_alarm_manager_plus` 的私有字段，整段包在 `catch (e: Exception)` 里只打日志。
   第三方库一升版本改字段名就静默失败 → 回调跑到了但发不出通知。
3. **重启不恢复**：旧 manifest 把 `RebootBroadcastReceiver` 显式写成 `android:enabled="false"`，
   与调度时传的 `rescheduleOnReboot: true` 自相矛盾，重启后闹钟全部丢失。
4. **时区静默回退 UTC**：`FlutterTimezone.getLocalTimezone()` / `tz.getLocation()` 失败就
   `tz.setLocalLocation(tz.UTC)`，只 `debugPrint`。而 `fireAt` 又用本地 `DateTime` 重建，
   判断 `isBefore` 用一个基准、真正调度用另一个，临近设定时刻会算出已过去的时间点。
5. **通知通道被删除重建**：旧原生 `ensureChannel` 发现通道 importance 不是 HIGH 就
   `deleteNotificationChannel` 再建。原生 `createNotificationChannel` 对已存在通道**只能改
   name/description，改不了 importance**。删除重建会覆盖用户对旧通道的设置；这本身不能证明它是提醒失效的原因。
   新通道 ID 用来避开旧通道遗留状态，创建后尊重用户设置。

### 对应硬约束

1. **不自建 Dart/AlarmManager 续订链** —— 用 `zonedSchedule` +
   `matchDateTimeComponents: DateTimeComponents.time`，由插件原生 receiver 管理重复登记；记录每次原生触发、续订和开机重排结果
2. **绝不用 `android_alarm_manager_plus`**、不用后台 Dart isolate、不用反射挂插件
3. **绝不把 boot receiver 写成 `android:enabled="false"`**
4. **绝不静默回退 UTC** —— 时区失败必须上报 UI 并拒绝调度
5. **绝不 `deleteNotificationChannel`** —— 只创建，不删除
6. **通道 id 换新为 `diary_reminder_v2`** —— 旧功能已在设备上建过 `diary_reminder` 通道，
   即使当时建成了非 HIGH，复用旧 id 也改不回来，症状是"能发但不弹横幅、不出声"，
   肉眼就是"没提醒"
7. 降级目标必须是 `AndroidScheduleMode.inexactAllowWhileIdle`。
   `exactAllowWhileIdle` 同样需要精确闹钟权限，用它等于没有降级，反而成了新的静默失败点
8. 不碰 `signingConfigs`（既有偏好）

## 三、外部事实核实

本方案锁定 `flutter_local_notifications` **22.3.1**，要求：

- Flutter ≥ 3.38.1（本机 3.47.4 ✓）
- compileSdk ≥ 35（`flutter.compileSdkVersion` = 36 ✓）
- AGP ≥ 8.11.1（`android/settings.gradle.kts` 正好 8.11.1 ✓）
- 需要 desugaring —— `android/app/build.gradle.kts:24,65` 已有
  `isCoreLibraryDesugaringEnabled = true` + `desugar_jdk_libs:2.1.4`，
  是上次功能**未回退**的遗留，本次正好复用，**无需改 build.gradle.kts**
- **插件 v16+ 自己只声明 `POST_NOTIFICATIONS` 和 `VIBRATE`**，其余权限与 receiver
  必须由 App 的 AndroidManifest 声明
- 文档明确警告：用 `@mipmap/ic_launcher` 当通知小图标时可能被 R8 裁掉导致**通知静默不显示**
- 文档明确点名 Xiaomi/Huawei 等国产 ROM 会限制后台执行
- Windows 不支持重复通知（`periodicallyShow` 抛 `UnsupportedError`）→ 功能必须 Android-only

## 四、文件清单

### 新增

| 路径 | 职责 |
| --- | --- |
| `lib/models/diary_reminder.dart` | `DiaryReminderSettings`（enabled/hour/minute，默认 21:00）+ `timeLabel` + `copyWith`；`DiaryReminderIssue` 枚举；`DiaryReminderStatus` |
| `lib/services/diary_reminder_service.dart` | 唯一编排点：时区校验、通道、`zonedSchedule`、取消、权限查询与申请、`pendingNotificationRequests` 自检、冷启动读取、点击分发、resume 幂等重排。内含 `DiaryReminderBackend` 抽象供测试注入 |
| `lib/services/diary_reminder_diagnostics.dart` | 通过 MethodChannel 读取/确认原生事件队列；幂等导入 `AppLogService`，保留实际发生时间；支持注入日志服务和通道以便测试 |
| `third_party/flutter_local_notifications/` | 从锁定的 22.3.1 版本维护最小本地 fork，只给日记提醒的原生触发、通知提交和下次登记结果写诊断事件 |
| `third_party/flutter_local_notifications/android/src/main/java/com/dexterous/flutterlocalnotifications/DiaryReminderNativeEventStore.java` | 本地插件 fork 内的有界事件队列；receiver 写入，`MainActivity` 读取/确认同一队列 |
| `android/app/src/main/res/drawable/ic_stat_diary_reminder.xml` | 单色通知小图标（24dp 白色铅笔矢量） |
| `android/app/src/main/res/raw/keep.xml` | `tools:keep` 保留通知图标，防 R8 裁资源 |
| `test/support/fake_diary_reminder_backend.dart` | 手写 fake（项目无 mockito），记录调用、可配置权限/时区/异常 |
| `test/diary_reminder_service_test.dart` | 服务层单测 |
| `test/diary_reminder_diagnostics_test.dart` | 原生事件队列读取、幂等写入和失败重试测试 |
| `test/diary_reminder_drawer_test.dart` | 抽屉 UI 与持久化测试 |

### 修改

| 路径 | 改动 |
| --- | --- |
| `pubspec.yaml` | 加 3 个依赖，并将 `flutter_local_notifications` 覆盖到本地 22.3.1 fork |
| `android/app/src/main/AndroidManifest.xml` | 加 2 个 permission + 2 个 receiver |
| `lib/widgets/profile_settings_drawer.dart` | 新增「提醒」分区 |
| `lib/main.dart` | 点击回调绑定、路由派发、冷启动读取、fire-and-forget 初始化 |
| `lib/screens/main_screen.dart` | `resumed` 分支先导入原生运行事件，再加 `ensureScheduled()` |
| `android/app/src/main/kotlin/com/example/time_manager/MainActivity.kt` | 增加读取/确认原生提醒事件的 MethodChannel |
| `lib/models/app_log_entry.dart` | 为导入的原生事件增加可选去重 ID（空值不序列化），旧日志兼容读取 |
| `lib/services/app_log_service.dart` | 增加带原始发生时间且返回持久化结果的幂等日志入口，避免原生事件只按下次启动时间排序或重复导入 |

**不改**：`android/app/build.gradle.kts`、`ProfileScreen`、
任何既有测试文件、`lib/theme/*`

### 依赖

```yaml
  flutter_local_notifications: ^22.3.1
  flutter_timezone: ^5.1.0
  timezone: ^0.11.1
```

本地 fork 使用依赖覆盖，保留上游 v22.3.1 的完整包结构与许可证文件：

```yaml
dependency_overrides:
  flutter_local_notifications:
    path: third_party/flutter_local_notifications
```

## 五、`DiaryReminderService` 设计

纯静态类，风格对齐 `UpdateService` / `AiPrivacySettings`。

### 常量

```dart
static const int notificationId = 2001;
static const String channelId = 'diary_reminder_v2';   // 必须换新，见硬约束 6
static const String channelName = '写日记提醒';
static const String payload = 'diary_reminder';
static const String smallIcon = 'ic_stat_diary_reminder';
static const int defaultHour = 21, defaultMinute = 0;

// 身份隔离 key（实际写入 identity_g_/identity_j_ 前缀）
static const String _enabledBaseKey = 'diary_reminder_enabled';
static const String _hourBaseKey = 'diary_reminder_hour';
static const String _minuteBaseKey = 'diary_reminder_minute';
@visibleForTesting static DiaryReminderBackend? backendOverride;
@visibleForTesting static bool? androidPlatformOverride;
@visibleForTesting static Future<String> Function()? timezoneIdentifierOverride;
```

平台门控用 `Platform.isAndroid`，**不用 `defaultTargetPlatform`** ——
flutter_test 默认把它当作 android，会污染既有测试。

### 错误上报通道（「绝不静默」的核心）

所有方法返回 `DiaryReminderStatus`，UI 只依据它渲染：

```dart
enum DiaryReminderIssue {
  none, unsupportedPlatform, identityUnavailable,
  timezoneUnavailable, notificationsDenied, channelDisabled,
  exactAlarmDenied, scheduleFailed,
}
```

`DiaryReminderStatus` 含 `settings` / `issue` / `message`（可直接显示的中文）/
`scheduled`（pending 中是否有 2001）/ `exactAllowed` / `notificationAllowed` /
`channelImportance` / `timezoneName` / `nextFireAt`。UI 不再依赖 `debugPrint` 判断成败。
`notificationAllowed` 与通道状态分开查询；通道被用户关闭时显示 `channelDisabled`，
重要性偏低时提示横幅/声音可能受限。

### 时区（硬约束 4）

```dart
static Future<void> _configureTimezone() async {
  tz_data.initializeTimeZones();
  String? id;
  try { id = await _timezoneIdentifier(); } catch (_) { id = null; }
  if (id == null || id.trim().isEmpty) {
    _timezoneName = null;
    return;                                         // 没有可信的当前时区，调用方拒绝调度
  }
  try {
    final loc = tz.getLocation(id);
    tz.setLocalLocation(loc);
    _timezoneName = loc.name;
  } catch (_) {
    _timezoneName = null;                           // 不用可能过期的缓存伪装成功
  }
}
```

### 调度（唯一注册入口，无任何回调续订）

```dart
await backend.zonedSchedule(
  id: notificationId,
  title: '写日记提醒',
  body: '该写日记了 — 记录今天的点滴吧',
  scheduledDate: next,                    // 必须 > now + 5s，否则插件抛 ArgumentError
  notificationDetails: const NotificationDetails(android: AndroidNotificationDetails(
    channelId, channelName,               // 前两位是位置参数
    channelDescription: '每天提醒写日记',
    importance: Importance.high, priority: Priority.high,
    category: AndroidNotificationCategory.reminder,
    icon: smallIcon, playSound: true, enableVibration: true, autoCancel: true,
  )),
  androidScheduleMode: exactAllowed
      ? AndroidScheduleMode.exactAllowWhileIdle
      : AndroidScheduleMode.inexactAllowWhileIdle,   // 硬约束 7
  matchDateTimeComponents: DateTimeComponents.time, // 硬约束 1：系统自己每日重复
  payload: payload,
);
```

> 参数名以实际解析到的包源码为准（v21+ 是具名参数风格），`flutter analyze` 会校验。

### 启动 / 重开恢复

`initialize()` 完成通知插件初始化、身份设置读取和时区校验后，先将 `_initialized` 置为 true，再调用
`ensureScheduled(force: true)` 一次。这样强制停止后重新打开、ROM 清除 pending 或进程重启时，
恢复不依赖 `resumed` 回调是否碰巧触发；开关关闭、身份未选或权限不满足时按状态返回并写诊断日志。
并发 `initialize()` 仍只初始化一次，启动补排也要并入同一排程锁。

### `ensureScheduled()` —— resume / 身份切换的幂等自检

```dart
static Future<DiaryReminderStatus> ensureScheduled({bool force = false}) async {
  if (!_isAndroid || !_initialized) return _status();      // 既有测试零影响
  if (!force && 距上次 < 30 秒) return _status();           // 防 resume 抖动
  ...
  if (!s.enabled) return _status(settings: s);
  final pending = await _safe(() => _backend.pendingNotificationRequests()) ?? const [];
  if (pending.any((p) => p.id == notificationId) &&
      _scheduledFingerprint == _currentFingerprint) {
    /* 配置未变且已登记；记录自检后返回 */
    return status;
  }
  return _schedule(s);                                     // 被 ROM 清掉了就补登记
}
```

### 排程自检日志（确认“已登记”）

`_schedule` / `ensureScheduled` / 测试通知后各写一条：

```dart
AppLogService.instance.info(
  '写日记提醒自检: enabled=…, issue=…, pending=…, exact=…, notif=…, tz=…, next=…',
  source: 'diary_reminder',
);
```

在「我的 → 运行日志」查看 `source=diary_reminder` 的记录；日志页按时间倒序显示，没有关键词搜索，
因此用来源和消息中的阶段名识别。点开日志行可看详情，或从日志页导出全部记录。
这能确认配置、排程调用和 pending 检查结果，但不能单独证明通知到点后显示成功。

排程日志至少区分 `schedule_attempt`、`schedule_registered`、`pending_verified` 和失败原因；
同时记录通知总开关、`diary_reminder_v2` 通道重要性、精确闹钟能力、时区、身份作用域和下次触发时间。

### 原生到点日志（确认“接收器运行到哪一步”）

到点时 Dart 不运行，`AppLogService` 不能直接收到回调。为让「运行日志」能在之后显示实际触发过程，
本方案维护锁定在 22.3.1 的本地插件 fork，并只在通知 ID `2001`、payload `diary_reminder` 上埋点：

1. `receiver_fired`：Android 的定时通知 receiver 已被系统调用。
2. `notify_returned` / `notify_failed`：插件调用通知管理器后未抛错，或捕获到异常。成功代表调用已提交，不代表用户一定看到或听到。
3. `next_schedule_returned` / `next_schedule_failed`：本次触发后，插件登记下一次提醒的结果。
4. `boot_reschedule_attempt` / `boot_reschedule_returned` / `boot_reschedule_failed`：开机 receiver 为日记提醒恢复排程的尝试和结果。
5. `notification_tapped`：用户点击后由 Dart 点击回调记录。

在锁定的 v22.3.1 源码中，埋点覆盖 `ScheduledNotificationReceiver` 的触发、通知提交和下次登记，
也覆盖 `ScheduledNotificationBootReceiver` 对 ID 2001 的开机恢复；插件内部捕获的精确闹钟异常
也必须转换为明确的失败事件，不能因插件吞掉异常而误记成成功。

原生 receiver 将事件写进独立 Android SharedPreferences 队列，使用同步提交，最多保留 50 条；
队列满时丢弃最旧事件并累计丢弃数，下次导入时写一条 warning，避免静默丢失。事件包含唯一 ID、
UTC 发生时间和结果码，不存通知正文或用户隐私。`MainActivity` 的 MethodChannel
提供读取与按 ID 确认接口。App 启动并完成 `AppLogService.initialize()` 后逐条导入事件；
`AppLogEntry` 保存原生事件 ID 作为 `dedupeKey`。调用新的 `logEventOnceAt()`，只有日志存储明确写入成功
才确认对应的原生队列项，失败时保留队列供下次重试。重复事件 ID 会复用既有日志而不重复新增，
日志使用原生事件的发生时间排序。
原生队列每次 `commit()` 都检查返回值；若本机存储提交失败，写 Android Logcat error 作为诊断兜底。
`importPendingEvents()` 使用 single-flight 锁，同一时刻只允许一次读取、写入和确认事务，避免冷启动与
`resumed` 恢复同时导入同一批事件。

如果同一事件已经在内存列表里、但上一次磁盘写入失败，重试必须先补做持久化，不能把“内存中已存在”
当成已经确认成功。成功事件记为 info，失败事件记为 error，权限或通道受限记为 warning。

**不能只调用现有 `info()` 后再 `flush()`**：当前 `AppLogService._enqueue()` 会捕获并吞掉写入异常，
`flush()` 因此无法告诉导入器持久化是否成功。新增的 `logEventOnceAt()` 必须返回写入结果，并在日志
存储失败时返回 false；幂等键写入日志 JSON，以处理“文件已追加但调用返回异常”的重试边界。

“通知管理器调用成功”仍不等于用户可见：Android 通知权限、通道重要性、勿扰模式和 ROM 设置都可能抑制提示。
这些日志可以定位到 receiver、通知 API 调用和重复登记阶段，不能证明横幅、声音或用户实际感知。
排程和原生触发日志要分别显示，不能将前者标成“已送达”。

### 身份隔离的两道护栏

依据实证（`app_identity_service.dart:198-203,153`）：

1. **未选身份时禁止开启**：`personKind == null` → `issue = identityUnavailable`，开关禁用。
   否则写入 `identity_unbound_*`，用户选完身份后设置"凭空消失"，成为新的静默失效源。
2. **`_prefs()` 只在 `!AppIdentityService.isLoaded` 时 `load()`**，且失败吞掉。
   因为 `_loadInternal` 结尾**无条件** `_notifyChanged()`，在 `changes` 监听里 `await load()`
   会构成 `load → notify → ensureScheduled → load` 死循环。

## 六、AndroidManifest 改动

**权限**（`android/app/src/main/AndroidManifest.xml:8` 之后）：

```xml
<!-- 写日记提醒：POST_NOTIFICATIONS / VIBRATE 由插件自带，这里只补定时通知必需项 -->
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
<!-- 侧载自用：安装即授予精确闹钟权限，无运行时弹窗；不使用 SCHEDULE_EXACT_ALARM -->
<uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
```

**receiver**（`:62` 之后、`:63` `</application>` 之前）：

```xml
<receiver
    android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
    android:exported="false" />
<receiver
    android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
    android:exported="false">
    <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
        <action android:name="android.intent.action.QUICKBOOT_POWERON" />
        <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
    </intent-filter>
</receiver>
```

**绝不写 `android:enabled="false"`**（硬约束 3）。不加 `ActionBroadcastReceiver`，
不加任何自定义 `*TapReceiver`（点击由插件的 `onDidReceiveNotificationResponse` +
`getNotificationAppLaunchDetails` 覆盖）。

### 精确闹钟的选择

选 `USE_EXACT_ALARM` 而非 `SCHEDULE_EXACT_ALARM`：

- 本 App 侧载自用、不经 Play 审核，`USE_EXACT_ALARM` 的应用商店政策约束不成立
- `USE_EXACT_ALARM` 安装即授予、终生有效、无弹窗
- `SCHEDULE_EXACT_ALARM` 在 targetSdk 36 下默认**拒绝**，必须跳到"闹钟和提醒"设置页手工打开。
  任何一次没点开，精确排程就会失败并走明确的降级路径——这正是最容易被误判成"功能没用"的形态

运行时仍保留 `canScheduleExactNotifications()` 判断与 `exactAlarmDenied` 降级作为防御。

## 七、通知图标与 keep.xml

现状：`res/` 下没有 `raw/`、没有任何单色通知图标。现有 `mipmap-*/launcher_icon.png`
是彩色自适应图标，Android 5+ 要求小图标为 alpha-only 单色剪影，直接用会渲染成灰块；
且它被 Dart 字符串间接引用，资源压缩看不到引用，可能整块裁掉 → 通知静默不显示。

新增：

- `res/drawable/ic_stat_diary_reminder.xml` —— 24dp 白色铅笔矢量，`fillColor="#FFFFFFFF"`
- `res/raw/keep.xml` —— `<resources xmlns:tools="…" tools:keep="@drawable/ic_stat_diary_reminder" />`

代码侧用 `AndroidInitializationSettings('@drawable/ic_stat_diary_reminder')` 与
`icon: 'ic_stat_diary_reminder'`。当前 release 未开 `isShrinkResources`，风险本低，
但 `keep.xml` 成本为零且是插件文档明确要求，作为回归保险保留。

## 八、UI 改动（`profile_settings_drawer.dart`）

### 插入位置

抽屉**只有「账户与数据」一个分区头**（`:61-71`），其余条目平铺到 `:235` 的「检查更新」。
若插在 `:125` 的 Divider 之后，「同步中心 / 外观模式 / 导出备份 / 运行日志」会被视觉归到
「提醒」分区下。

**正确位置：插在 `:229` 的 `const Divider(height: 1)` 之前**（「那年今日」`:220-228`
之后、「检查更新」之前），让「提醒」自带独立结尾，也不与后面的 divider 重复：

```dart
if (isAndroid) ...[
  const Divider(height: AppSizes.hairline),
  Padding(
    padding: const EdgeInsets.fromLTRB(
      AppSpacing.pageHorizontal,
      AppSpacing.lg,
      AppSpacing.pageHorizontal,
      AppSpacing.md,
    ),
    child: Text(
      '提醒',
      style: AppText.caption.copyWith(
        fontWeight: FontWeight.w600,
        color: colorScheme.onSurfaceVariant,
      ),
    ),
  ),
  _buildDiaryReminderSection(context),
],
```

导入现有设计令牌 `app_tokens.dart`，用 `AppSpacing`、`AppSizes`、`AppText` 和 `colorScheme` 设置样式；
`isAndroid` 取自 `:50` 既有变量，非 Android 整段不渲染。

### State 新增

`_ProfileSettingsDrawerState` 目前**没有 `initState`**（State 字段只有 `:41-42`），需新增：

```dart
bool _diaryReminderLoaded = false;
bool _diaryReminderBusy = false;
DiaryReminderStatus? _diaryReminderStatus;

@override
void initState() {
  super.initState();
  unawaited(_loadDiaryReminderStatus());
}
```

加载全部 try/catch，失败降级为 `DiaryReminderStatus.fallback()` 并记
`AppLogService.warning` —— **抽屉必须能打开**，异常不得抛进 `build`。

### 控件结构

| 控件 | 内容 |
| --- | --- |
| `SwitchListTile` | `secondary: Icons.edit_calendar_outlined`，标题「写日记提醒」 |
| `ListTile` | `leading: Icons.schedule_outlined`，标题「提醒时间」，`subtitle` 为 `HH:mm`，`trailing: chevron_right`，`showTimePicker` **不传 locale**（对齐既有 3 处先例） |
| 告警 `ListTile` | 仅 issue 非 none/unsupported 时显示，`Icons.warning_amber_rounded` + `status.message`，点按跳系统设置 |
| `ListTile` | 「发送测试通知」，副标题「立即验证通知权限、通道与图标是否正常」 |

开关副标题映射：

| 状态 | 文案 |
| --- | --- |
| `identityUnavailable` | 请先选择上方用户身份 |
| `timezoneUnavailable` | 系统时区异常，已暂停提醒 |
| 未开启 | 已关闭 |
| `notificationsDenied` | 通知权限未授予 |
| `exactAlarmDenied` | 已开启（可能延迟几分钟） |
| `!scheduled` | 已开启（未登记，点按修复） |
| 正常 | 已开启 · 每天 HH:mm |

开关流程要点：

- 开启时先 `saveSettings(enabled: true)` → 若 issue 非 none 则 `requestPermissions()` 重排
  → **若 `notificationsDenied` 则回滚为关闭**（不留下一个永远不会响的"已开启"）
- 全程 `_diaryReminderBusy` 互斥，异常记日志 + SnackBar「切换失败，请查看运行日志」
- SnackBar 内联 `ScaffoldMessenger.of(context).showSnackBar`，与抽屉既有写法一致
- 只用 `colorScheme` 语义色，不引入任何颜色/圆角/间距字面量 → `test/visual_system_test.dart` 不受影响

## 九、`main.dart` 集成

**初始化**（`_initializeAndRunApplication`，`:389` `HttpOverrides.global` 之后、
`:391` `runApp(` 之前）：

现有 `AppLogService.initialize()` 完成后，Android 启动流程调用
`DiaryReminderDiagnostics.importPendingEvents()`。导入器逐条调用 `logEventOnceAt()`，只有明确持久化成功
才按事件 ID 确认原生队列；不要依赖普通 `info()` + `flush()` 判断写入结果。

```dart
if (Platform.isAndroid) {
  unawaited(() async {
    try { await DiaryReminderService.initialize(); }
    catch (e, st) { appLogService.error('写日记提醒初始化失败',
        source: 'diary_reminder', error: e, stackTrace: st); }
  }());
}
```

fire-and-forget，失败不阻塞启动。

**点击绑定与路由派发**：复用 `:501-511` 的 Android-only `initState` 分支与
`:537-557` `_dispatchPendingHomeWidgetAction` 的 `rootNavigatorKey` +
`addPostFrameCallback` 重试范式。

```dart
DiaryReminderService.bindTapHandler(_openDiaryFromReminder);
```

`bindTapHandler` 在绑定瞬间补发尚未消费的点击（避免"main 里 initialize 早于 App State
绑定"导致点击丢失），未绑定时置 `_pendingTap`。

```dart
void _dispatchPendingDiaryReminderRoute() {
  if (!_pendingDiaryReminderRoute || !mounted) return;
  final navigator = rootNavigatorKey.currentState;
  if (navigator == null) {
    WidgetsBinding.instance.addPostFrameCallback((_) => _dispatchPendingDiaryReminderRoute());
    return;
  }
  _pendingDiaryReminderRoute = false;
  unawaited(navigator.push<void>(MaterialPageRoute<void>(
    settings: const RouteSettings(name: '/diary-reminder'),
    builder: (_) => const DiaryScreen(),
  )));
}
```

冷启动：`initState` 里 `addPostFrameCallback` → `handleColdStartNavigation()`
（内部 `_coldStartHandled` 保证只跑一次），异常写入 `AppLogService`，不只打 `debugPrint`。

**resume 钩子**（`main_screen.dart:114-124` 的 `resumed` 分支）：

```dart
unawaited(() async {
  try {
    await DiaryReminderDiagnostics.importPendingEvents();
  } catch (e, st) {
    AppLogService.instance.error('原生提醒日志导入失败',
        source: 'diary_reminder', error: e, stackTrace: st);
  }
  try {
    await DiaryReminderService.ensureScheduled();
  } catch (e, st) {
    AppLogService.instance.error('写日记提醒恢复自检失败',
        source: 'diary_reminder', error: e, stackTrace: st);
  }
}());
```

幂等检查：未初始化/非 Android 直接返回；30 秒节流；比较当前身份、提醒时间、时区和调度模式的
fingerprint 后再检查 pending。身份或时区变化时即使 ID 仍存在也要取消并重排。
从后台恢复时先导入原生队列，再做排程自检，使本次后台期间的触发结果能立即出现在「运行日志」。
订阅 `AppIdentityService.changes` 时不得在回调里调用 `load()`，避免触发加载通知循环。

## 十、测试计划

### `test/diary_reminder_service_test.dart`

| 用例 | 断言重点 |
| --- | --- |
| 默认值 | 关闭 / 21:00，且不触碰后端 |
| 开启排定 | `zonedSchedule` 恰 1 次；`matchDateTimeComponents == DateTimeComponents.time`；`scheduleMode == exactAllowWhileIdle`；`scheduledDate` 为 21:00 |
| 不自建 Dart 续订 | fake 验证业务代码只登记一次 `zonedSchedule`；此测试不声称覆盖 Android 原生 receiver 的每日续订 |
| 原生事件导入 | fake MethodChannel 返回触发/续订/开机事件；导入后日志使用原始发生时间，重复启动不重复导入 |
| 原生队列确认 | fake AppLogStore 先失败后成功时不提前确认；同一 `dedupeKey` 只持久化一条，确认失败可安全重试 |
| 日志阶段区分 | 排程登记成功只写 `schedule_registered`；原生 `receiver_fired`、`notify_returned`、续订和开机恢复结果分别映射为独立事件 |
| 幂等初始化 | 并发两次 `initialize()` → 后端 `initialize` 仅 1 次 |
| 启动补排 | enabled 且 pending 缺失时，首次 `initialize()` 主动补排；已存在时不重复登记 |
| 原生导入互斥 | 冷启动与 resume 同时请求导入 → 只执行一次队列读写确认事务 |
| **时区失败不静默降级** | 时区接口抛异常或返回无效地区 → `issue == timezoneUnavailable`、`zonedSchedule` 0 次 |
| 精确闹钟权限 false | `scheduleMode == inexactAllowWhileIdle`（**不是** exactAllowWhileIdle）、`issue == exactAlarmDenied` |
| 通知权限 false | `issue == notificationsDenied`，不调度 |
| 未选身份 | `issue == identityUnavailable`，且不写任何 prefs |
| 身份隔离 | 身份 g 写入 `identity_g_diary_reminder_*`；切到 j 读到 j 的值 |
| 关闭 | `cancel(id: 2001)` 被调用 |
| **通道不删除** | fake 不实现删除方法（编译期保证不会调 `deleteNotificationChannel`） |
| resume 修复 | pending 不含 2001 → 重排一次；含 2001 → 不重排 |
| 冷启动点击 | `didNotificationLaunchApp=true` + payload 匹配 → 导航回调恰好一次 |

### `test/diary_reminder_drawer_test.dart`

Android override + 注入 fake → 可见「写日记提醒」「提醒时间」「21:00」「发送测试通知」；
点击开关 → prefs 出现身份作用域 key 且 fake 收到一次 `zonedSchedule`；
`androidPlatformOverride = false` → 上述文案 `findsNothing`；
服务抛异常时抽屉仍能打开并显示降级文案。

### 保护既有 70 个测试

1. `initialize()` 只在 `main()` 调用且 Android 门控；新增 MethodChannel 用 fake 注入，不让既有测试触碰原生通知通道
2. 所有公开方法在 `!_initialized || !_isAndroid` 时同步短路，不产生平台调用
3. `ensureScheduled()` 在 `MainScreen` 里未初始化即返回，不引入未完成的异步
4. 抽屉新分区用 `if (isAndroid)` 门控，桌面路径完全不变
5. `_prefs()` 的 load 失败被吞，`profile_settings_drawer_test.dart` 在无 secure storage mock 时仍通过
6. **不修改任何既有测试文件**；若必须改，视为设计缺陷，回到上面修服务

## 十一、验证步骤

```bash
flutter pub get && flutter analyze
flutter test                                   # 既有 + 新增，全绿
flutter test test/visual_system_test.dart
flutter build apk --release

# 清单合并核对
grep -E "USE_EXACT_ALARM|RECEIVE_BOOT_COMPLETED|ScheduledNotification" \
  build/app/intermediates/merged_manifests/release/AndroidManifest.xml
# 资源未被裁掉
unzip -p build/app/outputs/flutter-apk/app-release.apk resources.arsc \
  | strings | grep -i ic_stat_diary_reminder
```

真机（每步是独立失败点）：

1. 装包后打开 App → 我的 → 抽屉 → 若未选身份，先选乖乖/晶晶（否则开关应置灰）
2. 打开「写日记提醒」→ Android 13+ 弹通知权限 → 允许；若拒绝，开关应**自动回滚为关闭**并提示
3. 「提醒时间」设为当前 + 2 分钟
4. 点「发送测试通知」→ 立刻收到（验证通道/权限/图标/keep 资源）。
   没收到则 `adb shell cmd notification get_notification_channels com.example.time_manager`
   看 `diary_reminder_v2` 的 importance 是否 4
5. 锁屏等到点 → 弹出（验证 `exactAllowWhileIdle` + Doze）；解锁回到 App 后，运行日志应依次出现 `receiver_fired`、`notify_returned` 和 `next_schedule_returned`
6. `adb shell dumpsys alarm | grep -i time_manager` → 应见 `RTC_WAKEUP`
7. 把提醒时间改为当前时间 + 15 分钟，重启手机并解锁；不手动打开 App，等到点后再启动 App。日志应能看到 `boot_reschedule_returned`、`receiver_fired` 和后续续订结果
8. 强制停止 App 后不要求提醒继续触发；重新手动打开 App，日志应显示再次登记，设为当前 + 2 分钟后验证恢复
9. 最近任务划掉但不强制停止，等到点 → 应照常弹出；回到 App 后导入原生事件
10. 进程被系统回收后点击通知 → push 出独立日记页（**不是**切到日记 Tab），返回后回到点通知前的页面
11. 前台切换身份或系统时区后，应取消旧排程并登记新排程；日志应带出新身份作用域 / 时区

### MIUI 需手动完成的系统设置

- 省电策略 → 无限制
- 自启动 → 允许
- 通知 → 允许，且「写日记提醒」通道设为重要 / 允许横幅
- 权限管理 → 闹钟和提醒 → 允许
- 其他权限 → 允许后台弹出界面
- 最近任务里给应用加锁
- 参考 dontkillmyapp.com 的小米条目

## 十二、风险与未知项

| 风险 | 验证方法 | 不成立时的对策 |
| --- | --- | --- |
| Java 11 vs 插件文档的 Java 17 | `flutter build apk --release` | 若 desugar/字节码报错，把 `build.gradle.kts:25-26,60` 升到 17（仅编译目标，不动 `signingConfigs`） |
| R8 裁掉通知图标 | 解压 APK 搜资源；真机测试通知是否显示 | `keep.xml` 已在；必要时显式 `isShrinkResources = false` |
| `USE_EXACT_ALARM` 是否安装即授予 | 自检日志 `exact=true`；`adb shell cmd appops get com.example.time_manager SCHEDULE_EXACT_ALARM` | 回退到 `SCHEDULE_EXACT_ALARM` + `requestExactAlarmsPermission()` 引导 |
| 插件能否在**无 Dart 进程**时靠 boot receiver 重排 | 第 7 步重启测试（不开 App） | 在启动早期调 `ensureScheduled(force: true)` |
| `matchDateTimeComponents: time` + `exactAllowWhileIdle` 组合行为 | 连续观察 2-3 天；或设 1-2 分钟后连续两次 | 改为每次 resume 用 `zonedSchedule` 重排（仍不用回调链） |
| 强制停止期间提醒不触发 | 第 8 步验证重新启动后的恢复，不把强制停止期间当作成功条件 | 文档说明系统停止状态；启动 App 后强制自检并补排 |
| MIUI 后台限制 | 第 5、7、9 步 | 只能靠用户系统设置，文档化 |
| `AndroidNotificationDetails` 参数位置/命名 | `flutter analyze` | 按报错微调 |

### 最可能再次失败的地方（按概率）

1. **通道 importance 卡在旧低档位** —— 故必须用 `diary_reminder_v2`；
   测试通知必须验证到**横幅/声音**，不只是通知栏有条目
2. **`AppIdentityService.load()` 死循环** —— 故 `_prefs()` 只在 `!isLoaded` 时 load，
   监听回调里绝不刷新身份
3. **降级模式选错** —— 故必须是 `inexactAllowWhileIdle`，
   且每次调度先查 `canScheduleExactNotifications()`

## 十三、收尾

- 更新 `AGENTS.md` 的 Services 清单（加 `DiaryReminderService`）与 `README.md` 功能列表
- 版本号用 `scripts/build_android.sh` 的既有流程 bump
- 按既有约定**每个逻辑单元单独提交**，建议 3 个提交：
  ① 依赖 + 模型 + 服务 + Manifest + 图标资源；② 抽屉 UI + `main.dart` 集成 + resume 钩子；③ 测试

## 十四、当前进度

### 已完成

- [x] 依赖：`flutter_local_notifications ^22.3.1` / `flutter_timezone ^5.1.0` / `timezone ^0.11.1`，并加 `dependency_overrides` 指向本地 fork
- [x] 本地插件 fork：`third_party/flutter_local_notifications/`（97 个文件），新增事件队列 + 在 3 个 Java 文件里埋点，改动清单见该目录 `FORK.md`
- [x] 原生侧：`MainActivity.kt` 增加 `com.example.time_manager/diary_reminder_events` 通道（`readPending` / `ack`）
- [x] Dart：`DiaryReminderSettings/Issue/Status`、`DiaryReminderService`、`DiaryReminderDiagnostics`
- [x] 日志设施：`AppLogEntry.dedupeKey` + `AppLogService.logEventOnceAt()`（返回持久化结果，不吞异常）
- [x] Manifest：`RECEIVE_BOOT_COMPLETED` / `USE_EXACT_ALARM` + 两个 `ScheduledNotification*` receiver（**未写** `enabled="false"`）
- [x] 通知资源：`res/drawable/ic_stat_diary_reminder.xml`（单色矢量）+ `res/raw/keep.xml`
- [x] UI：设置抽屉新增「提醒」分区（开关 / 提醒时间 / 告警行 / 发送测试通知）
- [x] 集成：`main.dart` 启动导入 + 初始化 + 点击路由；`main_screen.dart` `resumed` 时先导入再自检
- [x] 测试：新增 42 个（服务 23 / 诊断 11 / 抽屉 8），**全量 617 个通过**；`flutter analyze` 无问题；`visual_system_test` 通过
- [x] `flutter build apk --debug` 通过（验证 fork 的 Java 能编译）

### 未完成

- [ ] **真机验证全部未做**：方案第十一章的 11 步（尤其是第 7 步「重启手机不打开 App 也要响」）
- [ ] Windows / macOS 上未回归（功能已按 Android 门控，理论上零影响）

### 实施中相对本方案的必要偏差

1. **`analysis_options.yaml` 加了 `third_party/**` 到 exclude**。方案没提，但不加的话 `flutter analyze` 会去分析 vendored 插件的源码，产生大量无关 lint。
2. **埋点不止改 receiver，还改了 `FlutterLocalNotificationsPlugin.java`**。方案的文件清单只列了 receiver 与事件队列，但「下次登记成功/失败」的真实判定点在插件内部：`scheduleNextNotification` 会吞掉 `ExactAlarmPermissionException`，`zonedScheduleNextNotificationMatchingDateComponents` 在拿不到下次触发时间时会静默 return（这会让每天重复永久断链）。在 receiver 外层记成功会把这两种失败误报成成功。
3. **队列语义明确为「读取不清空 + 按 ID 确认」**。`readPending` 返回事件后不删除，`ack(ids)` 只移除已确认持久化的。这样 Dart 侧写日志失败时事件留在队列里等重试。最初写成了「读取即清空 + 失败回写」，会丢失事件原始内容，已改掉。
4. **事件队列类新增了一个枚举值 `DiaryReminderIssue.statusUnavailable`**。方案给的枚举里没有「读取设置本身失败」这个状态，而抽屉必须能在此情况下正常渲染并说明原因。
5. **fork 里排除了 `example/`（151 个文件）与插件自带 `test/`**。这两部分对功能无用，留着只会污染仓库与检查范围。
6. **抽屉「提醒」分区头用 `AppText.caption`（12px）+ `AppSpacing.pageHorizontal`（16）**，而既有「账户与数据」分区头是手写的 `fontSize: 13` / 左缩进 20。两者在同一抽屉里视觉上**不对齐**。方案要求用令牌，我按方案实现了，但这个不一致需要你决定：要么把旧头也迁到令牌，要么新头改成与旧头一致的数值。
7. **`dropped > 0` 的告警日志不做幂等**（用普通 `warning`）。它描述的是「发生过溢出」而不是某个具体事件，用幂等键会把后续真正的溢出也挡掉。
8. **测试里「时区失败」的断言改成「不排程 + `timezoneName` 为 null」**，而不是方案里写的 `tz.local.name != 'UTC'`——`tz.local` 本来就默认是 UTC 且从未被设置过，那个断言恒不成立，测不出任何东西。
9. `AndroidScheduleMode` 按修订版用 `exactAllowWhileIdle`（不是 `alarmClock`），降级 `inexactAllowWhileIdle`。

