# 待修复问题

> 本文件只保留**当前仍未完成**的项。
> 历史记录（约 40 条已完成条目，含 2026-09-14 全面审查与 2026-09-15 第二轮复核）已归档到
> `docs/archive/2026-09-修复问题归档.md`。`docs/*` 按既有约定不入库，归档仅存本地。

## 1. 英文界面完整本地化

- **优先级**：低（2026-09-17 用户表示"看起来也不是很重要"，暂缓）
- **模块**：多页面 UI
- **相关文件**：`lib/main.dart`

`lib/main.dart:569-577` 已声明 delegates 与 `supportedLocales`（`zh_CN` + `en_US`），
但仓库内**没有 `l10n.yaml`、没有 `.arb`、没有 `AppLocalizations`**，`flutter:` 段下也没有
`generate: true`（pubspec 里那两个 `generate: true` 属于 `flutter_launcher_icons`，与 l10n 无关）。

结果是**半本地化的混合状态**：框架组件跟随系统语言，应用自己的文字不跟随。英文设备上
`showDatePicker` / `showTimePicker` / `showDateRangePicker`（9 处，见
`check_in_screen.dart:205,215`、`global_search_screen.dart:222`、`travel_screen.dart:209`、
`check_in_detail_screen.dart:70,80`、`daily_review_screen.dart:338`、
`add_check_in_goal_screen.dart:546`、`add_target_screen.dart:123,554`）会是英文，
而调用它们的界面标题和按钮仍是中文。另外**没有语言切换入口**，只能跟随系统。

### 规模（2026-09-17 排查）

全项目含中文字面量约 **1980 行**，但要分三类：

| 类别 | 规模 | 处理 |
| --- | --- | --- |
| 用户可见 UI（`screens/` + `widgets/`） | ~830 行 | 要翻译 |
| 数据 / 协议（见下） | — | **绝不能翻译** |
| 开发者诊断（`debugPrint` ~67、`_recordAppError` ~47+、`FormatException`） | — | 不必翻译 |

按层分布：`providers/time_provider.dart` 524、`screens/` ~603、`services/` ~549、
`widgets/` ~227、`models/` ~57、`main.dart`+`utils`+`theme` ~20。

### 绝不能翻译的中文（中文即数据）

| 位置 | 内容 | 原因 |
| --- | --- | --- |
| `time_provider.dart:529,532` | `temporaryCategoryName='临时'`、`deletedCategoryName='已删除'` | 保留名，参与 `_deletedRelations` 的 eventName 匹配 |
| `target.dart:108` | `period ?? '每天'` | 持久化字段默认值 |
| `known_google_users.dart:8-9` | email → `'乖乖'`/`'晶晶'` | 身份显示名 |
| `voice_schedule_parser.dart`（52 行） | 中文关键词表 | 解析的就是中文语音输入 |
| Google 日历签名 | `'乖乖爱心晶晶'` | 识别签名，翻了认不出自家事件 |
| `daily_review_summary.dart` 等 | AI 提示词 | 翻了 AI 输出语言会变 |

### 三个难点

1. **UI 文案写死在 model / enum 层**：`sync_center_state.dart:14-20,44-53`、
   `app_log_entry.dart:15-17`、`global_search_result.dart:17-24,33-34`、
   `diary_search_result.dart:17`、`on_this_day_entry.dart:26-27`。
   最麻烦的是 `check_in_view_filter.dart:3-5`，中文是 **enum 的构造参数**
   （`all('全部')` / `guaiGuai('乖乖')` / `jingJing('晶晶')`），必须先把 label 搬出 enum。

2. **中文嵌在日期格式串里**：`check_in_map_screen.dart:327` 的 `DateFormat('M月d日 HH:mm')`、
   `diary_screen.dart:169,174` 的 `DateFormat('yyyy年M月d日')`。要换成 locale-aware pattern，
   并且**必须补 `initializeDateFormatting`**（全项目现在没有调用，带 locale 的 `DateFormat` 会抛异常）。

3. **测试锚在中文文案上（最大隐性成本）**：17 个测试文件、约 123 处
   `find.text('中文')` 断言。测试直接 `pumpWidget(MaterialApp(...))`，默认 locale 是 `en_US`；
   文案搬进 ARB 后这些断言会全部失败。改测试比搬文案更难。
   涉及：`check_in_goal_lifecycle_test`(15)、`global_search_screen_test`(14)、
   `target_detail_refresh_test`(13)、`schedule_overwrite_test`(11)、`app_log_screen_test`(10)、
   `brush_mode_test`(9)、`main_error_handler_test`(9)、`sync_center_test`(9)、
   `check_in_map_preview_test`(6)、`privacy_boundary_test`(5) 等。

### 若日后要做

完整方案 = `l10n.yaml` + `flutter: generate: true` + `app_zh.arb`/`app_en.arb` +
替换 ~830 处 UI 文案 + 从 model/enum 搬 label + 补 `initializeDateFormatting` + 改 ~123 处断言。

**低成本的局部替代**：若只是不想看到混合状态，把 `supportedLocales` 收成只有 `zh_CN`
（`main.dart:574-577`），框架组件也会回到中文，1 行改动。

## 2. 历史时间块的长期分页与上限

- **优先级**：中低
- **模块**：主页面、历史记录
- **相关文件**：`lib/providers/time_provider.dart`、`lib/screens/main_screen.dart`

2026-09-17 复核：原清单中与存储上限相关的多数项已完成 ——

- Tab 懒加载（`MainScreen._ensureTabBuilt`）✅
- 应用日志 2000 条上限（`AppLogService.defaultMaxEntries`）✅
- AI 对话 40 条 / 512 KB / 60 会话上限（`DailyReviewChatStore`）✅
- 打卡照片缓存淘汰最久未修改项（`CheckInPhotoCache`）✅
- 打卡目标预览随输入即时刷新 ✅

仍缺：**历史时间块本身的分页读取与存储上限**，以及大数据量启动、存储接近上限时的测试。

## 3. 补充测试

- **优先级**：低
- **模块**：测试体系

仍缺：

- Windows / macOS 打卡照片流程的集成测试
- 窄屏、大字体、语义树 UI 测试
- 大数据量启动与存储接近上限的测试

---

## 明确不做

- **高德瓦片端点的授权与配额**。瓦片源 `webrd{01-04}.is.autonavi.com/appmaptile` 是高德
  JS SDK 内部端点，不在开放平台公开文档内，属于未授权调用，没有配额与 SLA；第三方资料指出
  高德/百度开发者协议禁止未经许可抓取瓦片。个人自用、非公开分发，接受该风险。

  2026-09-17 用户确认日常使用地图正常，原记录的 HTTP 400 未复现。同日做了能通过代码
  消除的部分：把地图缩放限制在 `MapTileConfig.minZoom` `maxZoom`（3–18），使请求不超出
  瓦片服务实际提供的层级。服务端限流或封禁风险无法通过代码消除，不再跟进。

  相关文件：`lib/utils/map_tile_config.dart`、`lib/widgets/check_in_map_preview.dart`。

- **自动更新的原生发布签名验签**（APK / EXE / DMG 原生签名、Ed25519 manifest 验签、
  release 签名配置）。2026-09-17 用户再次确认不做签名相关改动。
  自动更新链路保留现有 SHA-256 校验即可，不再往上加验签层。
  相关文件：`lib/services/update_service.dart`。

- **安全类问题**（硬编码 Token/API Key 轮换、清理 git 历史中的密钥）。个人自用、私有仓库、
  非公开分发，暴露面有限。
  若安全项同时导致功能故障（如打卡 404 误判清空本地数据），仍按功能缺陷处理。

## 明确不在范围内

- 全局搜索只检索本地缓存中的 AI 摘要与对话，不触发网络；远端 AI 内容检索不在范围内。
