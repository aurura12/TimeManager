# 待修复问题

## 目标详情页编辑后未刷新

- **状态**：已完成
- **优先级**：中
- **模块**：目标 → 目标详情 → 编辑
- **相关文件**：`lib/screens/target_detail_screen.dart`

### 问题描述

从目标详情页进入编辑页面，修改目标名称、周期或目标值并保存返回后，详情页可能仍显示修改前的标题和信息。返回目标列表后重新进入详情页，才会显示最新内容。

### 复现步骤

1. 打开任意目标详情页。
2. 点击右上角编辑按钮。
3. 修改目标名称、周期或目标值并保存。
4. 返回详情页，观察页面标题和目标信息卡。

### 原因分析

`TargetDetailScreen` 使用初始化时传入的 `widget.target`。跳转到 `AddTargetScreen` 后没有等待编辑页面返回，也没有重新从 `TimeProvider` 读取更新后的目标，因此详情页继续使用旧对象。

### 建议修复

编辑页返回后重新读取当前目标，或让编辑页返回更新后的 `Target` 对象，再通过 `setState` 更新详情页。补充一个 widget 测试，验证编辑返回后标题、周期和目标值立即更新。

## 缺少统一的同步中心

- **状态**：已完成
- **优先级**：高
- **模块**：日程、日记、出行、打卡、分类与目标同步

### 问题描述

目前不同数据模块的同步入口和反馈比较分散，用户主要通过页面内的按钮、Snackbar 或局部状态判断同步结果，不容易快速确认哪些数据已经同步、哪些仍在等待、哪些发生了冲突或失败。

### 建议功能

新增统一的“同步中心”页面或底部面板，集中展示：

- 各数据模块的同步开关和当前状态
- 最后同步时间
- 待上传、待下载和失败数量
- 冲突数量及冲突详情
- 单模块重试和全部重试
- 查看同步日志
- 离线状态和恢复联网后的同步进度

### 交互要求

首页或“我的”页面提供统一入口，并在入口处显示待同步数量或异常标记。同步成功、失败和冲突应保留可查看的状态，而不只依赖短暂提示条。

## 缺少真正的全局搜索

- **状态**：已完成
- **优先级**：中高
- **模块**：时间记录、日记、出行、打卡、目标与 AI 复盘

### 问题描述

目前“全局搜索”主要搜索时间记录，日记有单独的搜索页面，出行、打卡、目标和 AI 复盘内容不能在同一个入口中检索。用户需要先猜测内容属于哪个模块，再分别进入对应页面搜索。

### 建议功能

建立统一搜索入口，支持搜索：

- 时间记录和分类/子事件
- 日记正文、日期和标签
- 出行地点与事件
- 打卡目标、备注和地点
- 目标名称
- AI 每日复盘内容

### 交互要求

支持按内容类型、日期范围和身份筛选；结果显示所属模块、日期和摘要；点击结果后直接跳转到对应详情或日期。搜索页还应保留最近搜索和常用关键词，并提供清晰的空结果状态。

## 桌面小组件增加快捷操作

- **状态**：已完成
- **优先级**：中
- **模块**：Android 桌面小组件

### 问题描述

当前桌面小组件主要用于展示日期、时间统计、主要分类、当前/下一项安排和待同步状态，用户仍需要打开 App 才能完成常用操作。

### 建议功能

在不破坏现有信息展示的前提下，增加快捷操作：

- 打开当前时间块或指定日期
- 快速记录当前时间
- 开始/结束计时
- 快速打卡
- 打开今日复盘
- 打开同步中心

### 交互要求

不同尺寸的小组件提供不同信息密度；操作后立即刷新小组件并显示成功或失败状态。每个快捷入口应支持深链接，直接进入对应页面，而不是统一跳转到 App 首页；离线时要明确提示并在恢复联网后继续同步。

## Windows/macOS 增加键盘快捷键

- **状态**：已完成
- **优先级**：中
- **模块**：Windows/macOS 桌面端

### 问题描述

桌面端适合键盘操作，但目前常用功能主要依赖鼠标点击页面按钮，长时间记录和频繁切换日期时操作效率不高。

### 建议快捷键

- `Ctrl/Cmd + Z`：撤销
- `Ctrl/Cmd + Shift + Z`：重做
- `← / →`：切换前一天/后一天
- `T`：回到今天
- `Ctrl/Cmd + K` 或 `Ctrl/Cmd + F`：打开搜索
- `Esc`：关闭弹窗、退出刷子模式或取消当前选择
- `Space`：开始/结束计时（如果后续加入计时功能）

### 交互要求

仅在输入框未获得焦点时响应全局快捷键，避免干扰文字编辑。菜单或帮助入口中显示快捷键说明，并兼容 Windows 的 `Ctrl` 与 macOS 的 `Cmd`；快捷键触发后应提供轻量反馈，避免用户无法判断是否生效。

## 2026-09-14 全面审查待修复项

以下问题来自本次功能、UI、数据一致性、平台适配和安全审查。未包含 API key/token 配置本身，也未包含发布签名配置问题。

### 自动更新链路缺少完整性保护

- **状态**：部分完成
- **优先级**：高
- **模块**：自动更新
- **相关文件**：`lib/services/update_service.dart`

### 问题描述

自动更新下载后没有校验 SHA256、发布签名或安装包完整性；下载地址、响应大小、版本号和文件名约束不足。Windows 安装程序的启动参数也需要避免通过字符串拼接交给 shell 解释。

### 建议修复

- 仅允许白名单 HTTPS 域名和预期的下载路径。
- 设置响应大小上限，校验 `Content-Length`，并在流式下载过程中持续限制实际字节数。
- 下载到临时文件，校验 SHA256 或发布签名、文件类型和扩展名后再安装。
- 对版本号和文件名做严格字符校验，防止路径穿越。
- Windows 使用参数化进程调用，避免拼接 `cmd` 命令。
- 安装失败时清理临时文件，并向用户显示可理解的错误原因。

已完成下载地址/路径、文件名、版本号、响应大小、流式大小限制、SHA-256
校验、临时文件清理和 Windows 参数化启动；APK/EXE/DMG 的原生签名校验仍需
配合发布签名与验签方案单独接入。

### 备份导入可能覆盖或清空已有数据

- **状态**：已完成
- **优先级**：高
- **模块**：备份导入
- **相关文件**：`lib/providers/time_provider.dart`、`lib/services/data_backup_service.dart`、`lib/widgets/profile_settings_drawer.dart`

### 问题描述

备份导入不是事务操作。格式错误的 `dailySlots` 可能被当成空数组，从而清空对应日期甚至全部历史时间块。导入文件还缺少文件大小、字段数量、字符串长度和嵌套深度限制。

当前界面提示“覆盖所有本地数据”，但实际备份范围并不包括全部日记、出行、打卡照片、日志和 AI 对话，容易造成用户误判。

### 建议修复

- 先导入到临时对象，完整校验通过后再一次性替换当前数据。
- 失败时回滚，不允许部分解析结果覆盖现有数据。
- 增加文件大小、数组数量、字符串长度和嵌套深度限制。
- 明确显示实际备份范围，并提供导入前预览和确认。
- 为损坏的每日时间块、空数组和旧版本格式补充回归测试。

### 出行记录修改日期和删除同步存在一致性问题

- **状态**：已完成
- **优先级**：高
- **模块**：出行记录
- **相关文件**：`lib/screens/travel_screen.dart`、`lib/models/travel_record.dart`

### 问题描述

编辑出行记录日期时只写入新日期，没有删除旧日期，因此会产生重复记录。删除操作没有远端 tombstone；远端删除失败后，下一次拉取可能恢复已删除记录。

### 建议修复

- 修改日期时使用旧日期和新日期进行原子迁移，避免重复或覆盖。
- 为删除操作增加 tombstone、版本号或其他可同步的删除标记。
- 增加离线删除、冲突、远端失败和重复日期的测试。

### 桌面端取消拍照打卡功能

- **状态**：已完成
- **优先级**：高
- **模块**：打卡照片
- **相关文件**：`lib/widgets/check_in_photo_sheet.dart`、`lib/services/check_in_image_service.dart`

### 问题描述

Windows/macOS 不提供拍照打卡能力。当前界面仍可能显示相机入口，用户点击后会进入不支持的桌面端相机流程。

### 建议修复

- 在 Windows/macOS 隐藏或禁用 `ImageSource.camera` 入口，不再提供拍照打卡功能。
- 保留相册选图和已有照片展示流程；如果后续决定桌面端完全不支持照片打卡，再同步隐藏照片入口和相关校验。
- 移动端继续保留拍照打卡功能。
- 补充平台 UI 测试，确认桌面端不显示相机按钮、移动端仍显示相机按钮。

### 远端照片路径和资源大小缺少防护

- **状态**：已完成
- **优先级**：高
- **模块**：打卡同步与照片缓存
- **相关文件**：`lib/services/check_in_photo_cache.dart`、`lib/models/check_in_record.dart`、`lib/services/check_in_gitee_service.dart`

### 问题描述

远端记录中的 `photo_path` 未做规范化和白名单校验。Windows 路径分隔符可能被利用构造路径穿越，导致缓存写入非预期位置。远端照片、压缩输入和下载响应也缺少统一大小限制。

### 建议修复

- 只允许仓库内固定目录、固定扩展名和安全文件名。
- 对路径进行规范化后确认仍位于目标缓存目录内。
- 限制单张照片、总缓存、响应体和解码图像尺寸。
- 删除远端记录或目标时同步清理关联照片，并处理上传成功但元数据提交失败的孤儿文件。
- 增加恶意路径、超大图片和损坏图片测试。

### 打卡身份、统计和业务规则需要统一

- **状态**：已完成
- **优先级**：高
- **模块**：打卡、身份切换、统计
- **相关文件**：`lib/models/check_in_record.dart`、`lib/models/check_in_goal.dart`、`lib/services/check_in_sync_service.dart`、`lib/screens/check_in_screen.dart`、`lib/screens/check_in_detail_screen.dart`

### 问题描述

手动身份和 Google 身份的 ID 不同，部分查询支持 email 回退，部分统计只按 ID 查询，身份切换后可能出现统计断裂或重复。“全部”筛选下目标卡片可能混入其他用户记录。

服务层对目标归属、目标起止日期、重复补打卡和目标是否已开始的校验不足，业务规则主要依赖 UI，容易被其他入口或异常请求绕过。

### 建议修复

- 定义稳定的身份归一化策略，统一所有查询、统计和同步入口。
- 明确“全部”筛选是聚合统计还是按目标所有者统计，并统一实现。
- 在 service 层校验目标归属、起止日期和每天最多一次等规则。
- 为身份切换、补打卡、重复提交、未来目标和离线冲突增加测试。

### 同步分页、响应上限和失败状态不完整

- **状态**：已完成
- **优先级**：中
- **模块**：Google 日历、日记和远程同步
- **相关文件**：`lib/services/google_calendar_service.dart`、`lib/services/diary_gitee_service.dart`、`lib/services/diary_search_service.dart`

### 问题描述

Google Calendar 和日记远端目录只处理单页结果，超过一页后会静默遗漏数据。多个远程接口和搜索下载流程没有统一的响应大小、文件数量和并发上限。

### 建议修复

- 完整处理 `nextPageToken` 或远端分页字段。
- 为响应体、文件数量、单文件大小和并发数设置上限。
- 对截断、分页失败和部分成功状态向用户保留可见状态。

### 地图加载失败和服务标识问题

- **状态**：已完成
- **优先级**：中
- **模块**：打卡地图
- **相关文件**：`lib/widgets/check_in_map_preview.dart`

### 问题描述

测试过程中高德瓦片请求多次返回 HTTP 400。需要在真实设备和生产网络环境复核，否则地图可能出现空白。地图组件还应确认是否满足服务 attribution 要求。

### 建议修复

- 增加瓦片请求失败、重试和空状态 UI。
- 复核瓦片 URL 参数、域名、配额和生产网络可达性。
- 按地图服务要求显示 attribution 和隐私说明。

### 隐私、备份、日志与 AI 数据需要明确边界

- **状态**：已完成
- **优先级**：中
- **模块**：本地存储、应用日志、AI 复盘、Android 备份
- **相关文件**：`android/app/src/main/AndroidManifest.xml`、`lib/services/app_log_store.dart`、`lib/services/app_log_export_service.dart`、`lib/services/daily_review_summary.dart`、`lib/services/siliconflow_ai_service.dart`

### 问题描述

日程、日记、位置、照片、错误堆栈和 AI 对话均可能以明文保存在应用目录、SharedPreferences 或导出文件中。Android 未明确配置备份策略，AI 复盘会将完整日程和分类信息发送到第三方服务，当前需要更明确的用户提示和数据控制。

### 建议修复

- 明确 Android 系统备份和设备迁移策略。
- 日志中避免保存原始响应、个人路径和敏感业务数据，导出前提供脱敏选项。
- AI 调用前显示数据范围、第三方服务和关闭入口。
- 为 AI 对话、日志和历史缓存设置数量、大小和保留期限。

### 启动性能、历史数据和缓存缺少上限

- **状态**：部分完成
- **优先级**：中
- **模块**：主页面、历史记录、AI 缓存
- **相关文件**：`lib/screens/main_screen.dart`、`lib/providers/time_provider.dart`、`lib/services/daily_review_chat_store.dart`

### 问题描述

所有 Tab 会在启动时预先创建，隐藏页面也可能开始同步和加载地图。历史时间块、日志、AI 对话和缓存长期增长，没有统一的数量或存储上限，可能造成启动变慢、内存增长或本地存储失败。

### 建议修复

- 延迟初始化未访问的 Tab 和网络功能。
- 对历史数据、日志、AI 对话、图片缓存设置保留策略。
- 对大数据量进行分页读取和增量序列化。
- 增加大数据量启动和存储接近上限时的测试。

### UI、无障碍和异常恢复问题

- **状态**：部分完成
- **优先级**：中低
- **模块**：多页面 UI
- **相关文件**：`lib/main.dart`、`lib/widgets/check_in_photo_sheet.dart`、`lib/screens/add_check_in_goal_screen.dart`、`lib/services/check_in_location_service.dart`

### 问题描述

- English locale 下应用正文仍大量显示中文。
- 颜色和图标选择缺少语义标签，部分点击区域偏小。
- 照片弹窗不可滚动，小屏或大字体下可能溢出。
- 图片选择异常返回时缺少 `mounted` 检查。
- 永久拒绝定位后没有直接打开系统设置。
- 全局错误页可能显示原始异常和路径，重启逻辑也不是真正的进程重启。
- 添加打卡目标的预览内容不会随输入即时刷新。

### 建议修复

- 补全本地化资源和英文 UI。
- 使用语义控件、明确标签和至少 48×48 的触控区域。
- 对照片弹窗、表单和错误页进行窄屏及大字体测试。
- 所有异步回调更新 UI 前检查 `mounted`。
- 为权限拒绝提供打开系统设置的恢复入口。
- 对用户显示友好的错误信息，详细异常仅写入受控日志。

### 数据生命周期和归档恢复

- **状态**：已完成
- **优先级**：中低
- **模块**：打卡目标和照片
- **相关文件**：`lib/screens/check_in_archive_screen.dart`、`lib/models/check_in_goal.dart`、`lib/services/check_in_sync_service.dart`

### 问题描述

恢复归档目标时，`archivedAt` 可能无法真正清空。删除目标会保留关联照片，造成远端和本地的孤儿隐私文件。

### 建议修复

- 修正 `copyWith` 对显式 `null` 的处理。
- 删除目标时清理关联照片，并保留失败重试或垃圾回收机制。

### 审查回归测试补充

- **状态**：部分完成
- **优先级**：中
- **模块**：测试体系

需要新增或加强：

- 自动更新恶意 URL、超大文件、错误哈希和路径穿越测试；
- 损坏备份、部分字段错误和导入回滚测试；
- 出行日期迁移、删除冲突和远端复活测试；
- Windows/macOS 打卡照片集成测试；
- 身份切换、全部筛选、重复补打卡和目标日期规则测试；
- Google Calendar 和远程目录分页测试；
- 窄屏、大字体、语义树和权限拒绝 UI 测试。

## 2026-09-15 第二轮复核回归项

本轮复核重点检查上一轮改动后的数据安全、同步状态和桌面端交互。以下问题已在本轮修复，并补充了对应回归测试。

### P0 数据丢失/不可用

- **单条损坏打卡记录阻断整份文档同步**：已完成。解析记录时按条目隔离异常，缺少时间戳或字段类型明显错误的记录会被跳过，其余记录继续同步。
- **删除日期无法重新记录**：已完成。出行记录增加 `updatedAt` 版本，重新记录严格晚于 tombstone 时自动撤销删除；无法判断新旧时保留冲突并提示用户确认。

### P1 功能回归

- **未来开始的打卡目标不可见**：已完成。主列表增加“未开始”分组，仍可查看、编辑和删除，但不会显示打卡入口。
- **次数目标只能打卡一次**：已完成。UI 和 service 层统一按目标周期累计次数，达到 `targetCount` 前允许继续打卡。
- **自动安装因缺少 SHA-256 必然失败**：已完成。发布脚本为 APK/EXE/DMG 生成同名 `.sha256` 文件；客户端读取并校验摘要，缺少可信摘要时降级为打开发布页手动下载，不再误报可自动安装。
- **桌面端 Esc 关闭表单**：已完成。根层 Esc 只处理可关闭的 `PopupRoute`，不会弹出添加/编辑页面；启动失败页的重试会重新执行启动初始化。

### P2 数据一致性

- **同步中心待同步数量被清零**：已完成。无可靠实时读取器的模块保留本次业务操作结果；打卡照片等部分失败状态不会被默认的 0 覆盖。
- **目标拉取失败显示为同步成功**：已完成。目标拉取、合并、推送均返回结构化结果，远端失败会显示失败并保留待上传状态。
- **删除目标多弹出一层页面**：已完成。手动删除和 Provider 通知只允许触发一次返回。
- **全局搜索按插入顺序截断**：已完成。索引上限采用按内容类型轮转，避免时间记录挤掉出行、打卡和日记。
- **清空搜索输入后旧结果回填**：已完成。清空时使异步搜索失效、停止加载并清除旧结果。
- **启动期日记索引缺失**：已完成。主页面启动后后台加载日记索引，覆盖“那年今日”和全局搜索未进入日记页的场景。

### P3 体验和恢复

- **历史分类搜索结果跳到错误日期页**：已完成，改为打开历史记录视图。
- **同步中心 busy 状态卡死**：已完成，业务操作结束后会回落到可重试状态。
- **照片缓存超过 100 MB 后拒绝新照片**：已完成，写入前淘汰最久未修改的旧照片，空间仍不足时才安全失败。
- **预发布版本比较错误**：已完成，保留并按 SemVer 比较 `-beta`/`-rc` 等预发布标识。
- **启动失败页重试不是真重试**：已完成，重试会重新创建启动期 Provider 并执行依赖初始化。

本轮验证：`flutter analyze` 无告警，`flutter test` 全部通过（520 项），更新元数据脚本测试通过。

## 当前仍需跟进

- 自动更新的 APK/EXE/DMG 原生发布签名验签仍需结合正式发布证书和制品流程接入；当前下载链路已完成地址、大小、文件名、SHA-256 和临时文件保护。
- 地图目前保留现有瓦片源；失败恢复、重试、归属和隐私说明已完成，生产环境地图源授权与配额仍需单独确认。
- 英文界面完整本地化、历史时间块的长期分页/上限、更多桌面端集成测试仍未全部完成。
- 全局搜索当前搜索本地缓存中的 AI 摘要和对话，不触发网络；远端 AI 内容检索不在范围内。
