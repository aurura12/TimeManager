# 手机端分类滑轮 · 方案（v2，基于代码现状修订）

> v1 稿的方向保留，以下 6 处按当前代码实现做了修正。未动任何代码，本文只是方案。

## 1. 目标

单击或拖选时间段 → 右侧自动升起级联双弧轮 → 从用户已配置的分类/子事件中选择 → 不对就转、对就关 → 复用现有撤销入口。

## 2. 现状：代码事实

| 事实 | 位置 |
|---|---|
| 手机端是**右栏**，不是底部快捷栏：Row = 55pt 时间轴 + 网格 + 100pt 分类侧栏 | `home_screen.dart:187-365` |
| 右栏不只有分类列表，顶部还挂着模板/昨天/语音添加入口（`TemplateBar`），下面是可拖拽排序、左滑删除的分类列表 | `home_screen.dart:439-521` |
| `TimeGrid` 目前只有 `onTapDown` / `onPanStart` / `onPanUpdate` / `onDoubleTapDown`，**没有 `onPanEnd`** | `time_grid.dart:78-87` |
| 单击格子 → 选中该格，再点右侧分类 = 写入并**清空选区**（现有行为；滑轮方案会替换手机端这条分配入口） | `home_screen.dart:116-144`、`_assignCategory` `:662-684` |
| 临时事件入口是弹窗输名字 | `_showTemporaryEventDialog` `:711` |
| `assignCategoryToSlots` 每次调用都 **无条件压快照**，并触发**落盘 + Google 日历同步** | `time_provider.dart:1611-1647`（`_saveSnapshot` `:1621`、`_saveData` `:1644`、`_scheduleCalendarSync` `:1640`） |
| 撤销栈上限 20，按日期分栈，靠 `_lastEditedDateKey` 定位最近编辑日 | `:1340`、`:1562-1609` |
| 本方案不引入「最近使用」；滑轮选项直接来自用户当前配置的分类和子事件 | 本方案约定 |
| 网格行高 45pt、纵向 padding 8pt，但 `_calculateIndex` 用的是**硬编码** `45.0` / `8.0`（数值恰好等于 `AppSizes.gridRow` / `AppSpacing.sm`，未引令牌） | `home_screen.dart:98-114` |
| `TimeSlot.categoryId` 指向具体分类 id；临时分类名常量为 `'临时'` | `time_provider.dart:190` |

几何（360dp 安卓屏）：`360 = 55（时间轴）+ 205（网格）+ 100（右栏）`，一列 ≈ 34.2pt。

## 3. 形态规格（修订）

```
┌─ AppBar 56 ─────────────────────────────────────┐
│                                                 │
│ ┌──┬─────────────────┬────────────────────────┐ │
│ │时│                 │  右栏 100              │ │
│ │间│     网格 205     │  （模板/语音/分类列表）  │ │
│ │轴│   6 列 ×34.2    │                        │ │
│ │55│                 │                        │ │
│ └──┴─────────────────┴────────────────────────┘ │
│                        ▲                        │
│          滑轮浮层 180pt ┘（覆盖整条右栏 + 网格最右 2 列）
└─────────────────────────────────────────────────┘
```

- **本质**：浮层不是「额外的浮窗」，而是**右栏临时变身滑轮**。视觉上从右缘滑出、盖住右栏。
- **代价**：滑动期间右侧「点分类写入」这条老路径整体不可用（不是「最右 1–2 列被遮」这么轻）。
- **宽度 180pt**（不是 170）：中文父名 3–4 字 + 子名 4 字在 170 里 ellipsis 概率高。
- **行高**：`itemExtent 36`，可见 5 项 = 180pt，面板含头部/操作区约 240pt。32pt 低于 `AppSizes.minTapTarget = 48`（`app_tokens.dart:86`），点选会很难点。
- **形状**：两个**平行**左凸弧轮（父在左、子在右），弯曲度 14pt 起。
- **级联**：左轮 = 用户当前配置的父分类（去掉「临时」）；右轮 = 该父 `subCategories`（该列表已排除 `hiddenSubCategories`，无需额外过滤），**首项固定为父分类本身**（实现「只用工作」）。父分类顺序沿用用户当前排序，子事件顺序沿用 `subCategories` 顺序。
- **初始位置**：如果选区内已有槽位全部属于同一个父/子事件，滑轮停在该事件；如果选区为空白或包含多个不同父分类，则停在用户分类列表第一项的父分类 +「仅父分类」。如果父分类相同但子事件混合，父轮停在该父分类，子轮显示「多个子事件」状态。以上初始位置都不自动写入，只有用户实际选定事件后才应用；不根据历史频次或最近使用推断。
- **必须保留「临时…」入口**：左轮去掉「临时」后，临时事件的手机端入口就没了。浮层顶部留一个入口按钮，走现有 `_showTemporaryEventDialog`。

## 4. 交互流程（修订触发条件）

```
单击完成（单槽）或拖选松手（多槽）
   └─ 每次新选区都弹 ─────→ 右栏变身双弧轮，从用户配置分类中初始化
          ├─ 不滚直接关     → 不写（没有真正选择事件）
          ├─ 左轮停稳        → 只切换父分类并重置右轮，不提交、不收起
          ├─ 点击右轮当前项  → 明确确认当前事件，立即写入当前选区并自动收起
          ├─ 右轮停 300ms    → 立即按原分类点击语义写入当前选区，随后自动收起
          └─ 右轮未确认时点浮层外 /返回 → 取消滑轮，不写入、不清空选区；可重新打开滑轮
```

手机端单槽和多槽统一走滑轮，不保留「单槽走旧分类栏、多槽走滑轮」的分叉。右侧分类列表仍可用于查看、展开、排序和管理分类，但不再作为手机端时间选区的主分配入口。

**触发条件统一**：手机端单击一个槽位和拖选多个槽位都属于“完成一次时间选区”，两者都打开同一个滑轮组件。写入完成后统一清空选区，因此不再依赖“选区从无到有”来决定是否弹出，也不会出现滑轮只弹一次的问题。

**手势冲突（必须处理）**：浮层打开期间要显式屏蔽网格手势。`TimeGrid` 的 `onTapDown` 会先于任何浮层遮罩的 `onTap` 命中，不屏蔽会出现「点外面关浮层的同时又把选区改了」。双击删块同理。

取消边界：右轮尚未最终停稳确认时，点浮层外或返回只关闭滑轮并保留当前选区，不产生写入和撤销记录；用户可以重新打开滑轮。开始新的单击或拖选时，才替换并清除旧选区。

手势优先级：单击用于打开单槽滑轮，拖选结束用于打开多槽滑轮，双击删除仍保留。需要防止双击的第一次点击误触发单槽滑轮，单击确认应等待双击判定结束或由统一手势状态机分流。

## 5. 写入 / 撤销 / 同步（v1 漏掉的实现要点）

`assignCategoryToSlots` 目前一次调用 = 一次快照 + 一次落盘 + 一次 Google 日历推送。滑轮滚动过程中会经过多个中间项，但只有右轮最终停稳的选项才是用户确认的事件。因此关键不是让每个中间项都走写入，而是确保最终停稳时只调用一次原有分类写入逻辑。

| 时机 | 动作 |
|---|---|
| 单击/拖选并打开滑轮 | 只保存选区和轮子状态；不压快照、不写入 |
| 滑轮滚动中 | 只更新轮子位置；中间项不修改时间槽位 |
| 点击右轮当前项或右轮最终停稳约 300ms | 调用一次原有分类写入逻辑，完成一次快照、落盘和同步；随后清空选区并自动收起滑轮 |
| 未完成选择就关闭/取消 | 不写入、不产生撤销记录 |

左轮只负责切换父分类；右轮滚动过程中的中间项不算已确认事件，右轮最终停稳才算选定事件。右轮第一项「仅父分类」也是完整事件，选定后立即写入、清空选区并自动收起滑轮。一次单击或一次拖选只产生一次撤销步骤；想修改结果时重新选择时间范围即可。

落地要求：

1. 不要在轮子初始化或滚动过程中的 `onSelectedItemChanged` 回调里直接调用 `assignCategoryToSlots`；点击右轮当前项或使用滚动结束/停稳判定确认，保证一次选区最终只调用一次。
2. 浮层和轮子状态不要订阅会导致自身重建的 `slotsRevision`，避免滚动过程中丢失 currentIndex。
3. 右轮确认后直接复用现有 `assignCategoryToSlots` 和 `undo()`；不额外改变原有分类写入、撤销和同步语义。注意 `_remoteViewEnabled` 下写入和撤销仍被 gate 掉。

撤销入口：不新增底部提示条，直接复用现有 AppBar 的「撤销」按钮（`home_screen.dart:1361-1363`），由它调用 `provider.undo()`。滑轮确认后只完成一次原有分类写入，不改变撤销入口和撤销语义。

## 6. 分类来源与初始值

滑轮不新增「最近使用」或频次统计能力，选项完全来自用户当前配置：

- 父轮读取当前分类列表，排除保留的「临时」分类，顺序沿用用户排序；
- 子轮读取当前父分类的 `subCategories`，顺序沿用用户配置；
- 子轮第一项固定为父分类本身，表示「仅使用父分类」；
- 选区内已有槽位全部属于同一个父/子事件时，初始位置跟随该事件；
- 选区为空白或包含多个不同父分类时，初始位置使用用户分类列表第一项 +「仅父分类」；
- 如果选区属于同一个父分类但包含多个子事件，父轮跟随该父分类，子轮显示「多个子事件」状态，并在浮层提示选区包含多个事件；
- 混合选区不能伪装成某个已有事件；初始位置仅是光标位置，用户实际选定后才应用新事件；
- 分类列表为空时，需要定义空状态和返回旧分类栏路径，不能让滑轮出现空轮。

## 7. 数据前置：合并两个「工作」

同意合并，但方向需要先确认，**不只是去重**：

- `TimeSlot.categoryId` 指向具体 id，两个「工作」选到不同 id → 历史统计**永久分流**；
- 合并前要给出：两者各被多少条历史 slot 引用（决定保留哪个 id、把另一批 `categoryId` 改写过来）、`subCategories` 并集后有无同名、颜色取哪个；
- 这一步的输出是「改前 → 改后」清单，确认后再动数据。

## 8. 样机：只做 B + C

| 版本 | 做法 | 结论 |
|---|---|---|
| A | 原生 `ListWheelScrollView` + `diameterRatio` / `perspective` / `offAxisFraction` | 作为无第三方依赖的基线，先验证滚轮曲率、可读性和停稳回调；如果侧向弧形不足，再比较第三方方案 |
| B | `curved_list_wheel` 包（`CurvedListWheelSide.left` + `pathCurveFactor`） | 最接近目标形态，优先出 |
| C | 位置变体：贴拖选跨度右侧、垂直居中 | 与 B 只差定位，同一份代码加个 flag 即可 |

- 宽度（180 / 190）和弯曲度（14 / 更弯）**不做成独立版本**，样机里加 ± 按钮现场抠。
- 想要更强的弧感，优先调「`itemExtent` 降到 ~28、可见 7 项」而不是加曲率——采样点变多比曲线变陡更有效。
- 「更弯」 vs 「更能读出弧」这两个变量在样机上容易混，实测时先看 7 项版本。
- 位置**先推 C**：不遮正在改的那一段才是这套交互的全部价值；B 固定半屏高时很容易正好压住选区。C 的代价是要重算跨度几何（现有 `_calculateIndex` 是硬编码常量），且选区贴顶/贴底要 clamp。

## 9. 分期

1. 出「两个『工作』」改前→改后清单，**确认后才动数据**（含 `categoryId` 改写）
2. `TimeGrid` 加单击确认、`onPanEnd`、`onPanCancel` 和双击优先级分流；**桌面三列视图不动**（现 `_DayGrid` 的选中逻辑保持）
3. 先补单击/拖选共用的停稳判定和双击分流测试，确认右轮只调用一次现有分类写入逻辑；不新增业务层会话 begin/commit API
4. 样机页：B/C 切换 + 宽度/曲率/itemExtent ± 调节（临时文件，跑完即删、还原 `main.dart`）
5. 按手机实测结论落地生产组件 + 复用现有 AppBar 撤销入口

## 10. 未决（等手机实测）

- 弯曲度 14pt 还是更弯 / 可见 5 项还是 7 项
- 宽度 180 还是 190
- B（固定右侧）还是 C（贴跨度垂直居中）
