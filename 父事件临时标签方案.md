# 父事件临时标签方案

## 背景与问题

个人中心（ProfileScreen）的"父事件"视图统计来自 `TimeProvider.getParentStatistics`（`lib/providers/time_provider.dart:6939`）。原始逻辑：

- label 是某分类 `subCategories` 中的子事件 → 并入该父事件
- 否则（非子事件）→ **作为独立项直接显示**

导致：通过首页"临时"对话框创建的临时事件（label 为任意输入名、categoryId 不在 `_categories` 中）各自成为独立的"父事件"扇区，污染父事件饼图。该项已实现（见"已实现部分"）。

现在追加两个问题：

1. **已删除的子事件**：在"编辑事件"弹窗里点"删除"移除子事件后，历史时间块仍带着该 label，但 label 已不在任何分类的 `subCategories` / `hiddenSubCategories` 中，于是被当成无归属标签塞进"临时"。
2. **整体删除的父事件**：`deleteCategory`（`:6539`）把分类移除后，父名和它的子事件名一起失去归属，同样落进"临时"，且原本同属一个父事件的历史被打散成多个临时标签。

关键约束：**删除后 label 会退化成无归属标签，从数据上跟"临时"无法区分**，因此必须在删除时本地记一份"已删除关系"记录。记录不能只存标签名，还要保存删除时的父事件、事件类型和分类 ID，供“已删除”明细恢复父子关系。

## 目标行为

1. 子事件 / 隐藏子事件（`hiddenSubCategories`）→ 计入其父事件。
2. **已删除的事件（含被删除的子事件、被整体删除的父事件及其子事件）→ 统一归入"已删除"聚合项**，不再各自独立显示，也不混进"临时"。
3. 其余无归属 label → 归入"临时"聚合项。
4. 优先级：**存活的分类关系（父名 / 子事件 / 隐藏子事件）> 已删除 > 临时**。同名重建的分类或子事件会让已删除记录失效。
5. **"全部"视图提供开关**，让用户选择是否把已删除标签合并成"已删除"一项；不合并时各 label 按原样独立显示（符合"全部"语义）。
6. 点击"已删除"项可查看全部已删除事件的明细。
7. **"已删除"明细必须保留父子关系**：不能只显示一串平铺的标签。子事件删除后显示为`父事件 / 子事件`；整体删除父事件时显示父事件本身，并在其下列出可见和隐藏子事件。

## 修订后的实施原则

本方案仍采用“只记录标签名、仅本地持久化、不改远端分类文档”的模型，但补充以下约束，避免实现后破坏已有的分类关系：

1. **活跃关系优先于删除记录**：统计、全部视图和明细都使用“有效已删除关系”——删除记录中当前仍属于父事件、可见子事件或隐藏子事件的事件全部排除。
2. **已删除记录不是标签平铺表**：每条记录至少保存 `categoryId`、删除时的父事件名、事件名、事件类型（父事件本身/子事件）和删除时间；这样“已删除”明细才能恢复父子层级。
3. **只记录真正消失的子事件**：`subCategories` 和 `hiddenSubCategories` 视为一个完整的活跃子事件集合。可见子事件移到隐藏区不算删除，隐藏子事件恢复也不算重新创建。
4. **改名与删除分开处理**：已有 `_propagateLabelRename` 能传播历史记录的纯改名不加入删除关系；只有旧标签从可见/隐藏集合中真正消失时才加入。父事件改名时同步更新已删除关系的父名快照。
5. **聚合名是保留名**：禁止删除“临时”分类，禁止新建名为“已删除”的父分类或子事件，避免真实标签与聚合项无法区分。
   临时事件快捷录入和语音写入也不得使用“已删除”作为历史标签；“临时”仍按既有无归属标签兼容处理。
6. **备份属于本地持久化范围**：备份导出/导入要携带删除关系记录；远端 Gitee 分类文档仍不携带该字段。

## 已实现部分（无需重做）

- `getParentStatistics`（`time_provider.dart:6939`）：`subCategories` + `hiddenSubCategories` → 父事件；其余 → `temporaryCategoryName`（'临时'）。
- `getTemporaryLabels([start, end])`（`:6981`）：收集无归属标签；名为"临时"的分类不计入已知集合，使恰好叫"临时"的标签也被视为临时事件。
- `getStatisticsWithTemporaryGrouped`（`:7017`）："全部"视图把临时标签合并为"临时"一项。
- `getEventHistory`（`:7075`）：`eventName == '临时'` 时展开为 `getTemporaryLabels()`；否则展开父事件的子事件 + 隐藏子事件。
- ProfileScreen（`:587-590`）按 `_showParentOnly` 在上述两个方法间切换；该开关是内存态，不持久化。

## 本次新增：已删除事件统一归入"已删除"聚合项

### 1. 数据模型

新增保留名常量（与 `temporaryCategoryName`（`:190`）并列）：

```dart
/// 已删除事件的聚合展示名（与"临时"同为保留名，不要求是真实分类）
static const String deletedCategoryName = '已删除';
```

分类创建/编辑入口要校验保留名：`temporaryCategoryName`（`'临时'`）仍由系统保留且不可删除；`deletedCategoryName`（`'已删除'`）不可作为父分类名或子事件名。校验失败时沿用现有弹窗提示方式，不写入分类。

在 `lib/models/deleted_event_relation.dart` 新增一个轻量模型，并在 `TimeProvider` 中维护关系记录：

```dart
class DeletedEventRelation {
  final String categoryId;
  final String parentName; // 删除时的父事件名；父事件仍存活时可解析为当前名称
  final String eventName; // 对应历史 TimeSlot.label
  final bool isParentEvent; // true：父事件本身；false：子事件
  final int deletedAt;

  const DeletedEventRelation({
    required this.categoryId,
    required this.parentName,
    required this.eventName,
    required this.isParentEvent,
    required this.deletedAt,
  });

  String get relationText =>
      isParentEvent ? parentName : '$parentName / $eventName';

  Map<String, dynamic> toJson() => {
        'categoryId': categoryId,
        'parentName': parentName,
        'eventName': eventName,
        'isParentEvent': isParentEvent,
        'deletedAt': deletedAt,
      };

  static DeletedEventRelation? fromJson(Map<String, dynamic> json) {
    // 必填字符串/布尔/时间戳校验失败时返回 null，由调用方记录并跳过坏记录。
  }
}

/// 已删除事件的关系记录，仅本地持久化，不参与远端分类文档合并。
/// 通过 (categoryId, isParentEvent, eventName) 去重并更新。
final List<DeletedEventRelation> _deletedRelations = [];
```

父事件整体删除时写入一条 `isParentEvent: true` 的父事件记录，以及每个可见/隐藏子事件各一条 `isParentEvent: false` 的子事件记录。只删除一个子事件时，只写入该子事件与其父事件的关系。这样统计仍可把所有时长汇总成一个“已删除”扇区，但点进去后能按父事件分组显示：

```text
已删除
├─ 工作
│  ├─ 写代码       09:00 - 10:00
│  └─ 开会         14:00 - 14:30
└─ 阅读             20:00 - 21:00   （父事件本身）
```

不再使用上一版的 `Set<String> _deletedLabels` 作为事实来源；标签集合只能由关系记录派生，避免为了统计而丢失父子结构。

为了保证“存活的分类关系 > 已删除”，不要在读取处直接使用 `_deletedRelations`。增加以下内部辅助方法：

```dart
Set<String> _liveCategoryLabels() {
  final labels = <String>{};
  for (final cat in _categories) {
    labels.add(cat.name);
    labels.addAll(cat.subCategories);
    labels.addAll(cat.hiddenSubCategories);
  }
  return labels;
}

List<DeletedEventRelation> _effectiveDeletedRelations() {
  final liveLabels = _liveCategoryLabels();
  return _deletedRelations
      .where((relation) => !liveLabels.contains(relation.eventName))
      .toList();
}

Set<String> _effectiveDeletedLabels() {
  return _effectiveDeletedRelations()
      .map((relation) => relation.eventName)
      .toSet();
}

String _deletedRelationParentName(DeletedEventRelation relation) {
  // 父事件还存活时使用当前名称；父事件已删除时退回删除时快照。
  Category? current;
  for (final cat in _categories) {
    if (cat.id == relation.categoryId) {
      current = cat;
      break;
    }
  }
  return current?.name ?? relation.parentName;
}

bool _containsReservedCategoryLabel(Category category) {
  final labels = <String>{
    category.name,
    ...category.subCategories,
    ...category.hiddenSubCategories,
  };
  return labels.contains(temporaryCategoryName) ||
      labels.contains(deletedCategoryName);
}

void _upsertDeletedRelation({
  required String categoryId,
  required String parentName,
  required String eventName,
  required bool isParentEvent,
  int? deletedAt,
}) {
  // 同一分类、同一类型、同一事件只保留一条，避免先删子事件再删父事件时重复。
  // 已存在的子事件关系不能被父事件删除流程覆盖掉其父子类型。
}

void _removeDeletedRelationsForEvent(String eventName) {
  _deletedRelations.removeWhere((relation) => relation.eventName == eventName);
}

void _renameDeletedRelationsParent(
    String categoryId, String oldName, String newName) {
  // 更新该 categoryId 下关系记录的父名快照；父事件自身记录的 eventName 也同步改名。
}
```

`_effectiveDeletedRelations()` 是“已删除”明细的读取入口，`_effectiveDeletedLabels()` 只是统计分支从关系记录派生出的标签集合。这样即使远端分类同步重新带回同名事件，或某个边界路径留下旧记录，存活关系仍然优先，同时不会丢失父子关系。

### 2. 写入 / 清理时机

**(a) `updateCategory`（`:4914`，编辑弹窗"保存修改"走这里）** —— 需要同时处理改名、删除和“可见 ↔ 隐藏”移动。当前编辑器会先在本地列表中修改 `subCategories` / `hiddenSubCategories`，保存时统一调用 `updateCategory`，不能只观察 `subCategories`。

```dart
if (_containsReservedCategoryLabel(updated)) return;

final oldVisibleSubs = oldCategory.subCategories;
final newVisibleSubs = updated.subCategories;
final renamedOldSubs = <String>{};

// 保留现有的等长度、按位置改名传播行为；这些旧名不是“删除”。
if (oldVisibleSubs.length == newVisibleSubs.length) {
  for (int i = 0; i < oldVisibleSubs.length; i++) {
    if (oldVisibleSubs[i] != newVisibleSubs[i]) {
      _propagateLabelRename(
        categoryId,
        oldVisibleSubs[i],
        newVisibleSubs[i],
      );
      renamedOldSubs.add(oldVisibleSubs[i]);
    }
  }
}

// 可见和隐藏都是“仍然存活”的子事件；只有从两者并集中消失才算删除。
final oldActiveSubs = <String>{
  ...oldCategory.subCategories,
  ...oldCategory.hiddenSubCategories,
};
final newActiveSubs = <String>{
  ...updated.subCategories,
  ...updated.hiddenSubCategories,
};
for (final sub in oldActiveSubs.difference(newActiveSubs)) {
  if (!renamedOldSubs.contains(sub)) {
    _upsertDeletedRelation(
      categoryId: categoryId,
      parentName: updated.name,
      eventName: sub,
      isParentEvent: false,
    );
  }
}
// 重新添加、改名而来、移动到隐藏区或从隐藏区恢复的子事件都优先存活。
for (final sub in newActiveSubs) {
  _removeDeletedRelationsForEvent(sub);
}
```

该方法末尾已有 `_markCategoriesChanged()`，持久化见第 4 节。

**(b) `deleteCategory`（`:6539`）** —— 在 `_categories.removeAt(index)` **之前**，把父名与其子事件一起记为已删除：

```dart
if (index < 0 || index >= _categories.length) return;
final cat = _categories[index];
if (cat.name == temporaryCategoryName) return; // 系统临时分类不可删除
final nowMs = DateTime.now().millisecondsSinceEpoch;
_upsertDeletedRelation(
  categoryId: cat.id,
  parentName: cat.name,
  eventName: cat.name,
  isParentEvent: true,
  deletedAt: nowMs,
);
for (final sub in {
  ...cat.subCategories,
  ...cat.hiddenSubCategories,
}) {
  _upsertDeletedRelation(
    categoryId: cat.id,
    parentName: cat.name,
    eventName: sub,
    isParentEvent: false,
    deletedAt: nowMs,
  );
}
```

已有的 `_deletedCategories[categoryId] = nowMs`（远端墓碑）保持不动。

**(c) `addCategory`（`:4901`）** —— 新建分类若与某已删除事件同名，从关系记录移除（存活分类优先）：

```dart
if (_containsReservedCategoryLabel(category)) return;
_removeDeletedRelationsForEvent(category.name);
for (final sub in {
  ...category.subCategories,
  ...category.hiddenSubCategories,
}) {
  _removeDeletedRelationsForEvent(sub);
}
```

**(d) `updateCategory` 改名** —— `oldCategory.name != updated.name` 分支（已有 `_propagateLabelRename`）同时更新该分类下已删除关系的 `parentName`。父事件纯改名不创建删除关系；历史时间块已由 `_propagateLabelRename` 改成新名。子事件纯改名由上面的 `renamedOldSubs` 排除，避免把改名误记为删除。

```dart
if (oldCategory.name != updated.name) {
  _propagateLabelRename(categoryId, oldCategory.name, updated.name);
  _renameDeletedRelationsParent(
    categoryId,
    oldCategory.name,
    updated.name,
  );
  _removeDeletedRelationsForEvent(updated.name);
}
```

**(e) `moveSubCategory`（`:6467`）** —— 子事件被移动到别的分类，它是"活着"的子事件：`_removeDeletedRelationsForEvent(subName);`。如果未来支持移动隐藏子事件，也必须对隐藏集合做同样处理。

**(f) `hideSubCategory` / `restoreSubCategory`（`:4947` / `:4965`）** —— 隐藏不等于删除；直接调用这两个方法时不加入关系记录，重新激活时可执行 `_removeDeletedRelationsForEvent(subCategory)`。当前编辑器的拖拽/恢复路径通过 `updateCategory`，由上面的可见+隐藏并集逻辑统一处理。

**(g) 清空数据（`:675`）** —— `_deletedRelations.clear()`。

**(h) 远端分类合并（`:3291`）** —— `_applyMergedCategories` 仍不把 `_deletedRelations` 写入 `CategoryDocument`，但应用合并后的存活分类后应清理当前活跃标签对应的本地关系记录，或依赖 `_effectiveDeletedRelations()` 过滤；推荐两者都做，避免关系长期残留。

### 3. 统计读取

**`getParentStatistics`（`:6939`）** —— 在现有"父事件 / 临时"两分支之间插入"已删除"分支：

```dart
final effectiveDeletedLabels = _effectiveDeletedLabels();

detailStats.forEach((label, hours) {
  final parentName = childToParent[label];
  if (parentName != null) {
    // 子事件（含隐藏）：累加到父事件
    parentStats[parentName] = (parentStats[parentName] ?? 0) + hours;
  } else if (parentNames.contains(label) &&
      label != temporaryCategoryName &&
      label != deletedCategoryName) {
    // 父事件本身：直接添加（两个保留名除外，统一归入各自的聚合项）
    parentStats[label] = (parentStats[label] ?? 0) + hours;
  } else if (effectiveDeletedLabels.contains(label)) {
    // 已删除事件：统一归入"已删除"聚合项
    parentStats[deletedCategoryName] =
        (parentStats[deletedCategoryName] ?? 0) + hours;
  } else {
    // 真正的临时事件（无归属）
    parentStats[temporaryCategoryName] =
        (parentStats[temporaryCategoryName] ?? 0) + hours;
  }
});
```

> `childToParent` / `parentNames` 的构建逻辑不变，仍只来自存活分类；`effectiveDeletedLabels` 还会过滤存活父名、子事件和隐藏子事件，所以同名重建或远端恢复都天然优先于"已删除"。

**`getTemporaryLabels`（`:6981`）** —— `known` 集合加入已删除标签，否则它们会同时出现在"已删除"和"临时"里：

```dart
known.addAll(_effectiveDeletedLabels());
```

**`getStatisticsWithTemporaryGrouped`（`:7017`）** —— "全部"视图按开关决定是否合并已删除：

```dart
Map<String, double> getStatisticsWithTemporaryGrouped(
  DateTime start,
  DateTime end, {
  bool groupDeleted = true,
}) {
  final stats = Map<String, double>.from(getStatistics(start, end));
  final deletedLabels = _effectiveDeletedLabels();

  if (groupDeleted) {
    var deletedHours = 0.0;
    for (final label in deletedLabels) {
      deletedHours += stats.remove(label) ?? 0;
    }
    if (deletedHours > 0) stats[deletedCategoryName] = deletedHours;
  }

  final temporaryLabels = getTemporaryLabels(start, end);
  var temporaryHours = 0.0;
  for (final label in temporaryLabels) {
    temporaryHours += stats.remove(label) ?? 0;
  }
  if (temporaryHours > 0) stats[temporaryCategoryName] = temporaryHours;
  return stats;
}
```

> 顺序：先扣有效已删除、再算临时。`getTemporaryLabels` 已把有效已删除标签算进 `known`，两者不会重叠；`groupDeleted: false` 时不扣除已删除标签，它们按原 label 独立显示。

**`getEventHistory`（`:7075`）** —— 支持点开"已删除"聚合项看明细：

```dart
Set<String> validLabels;
final allDeletedRelations = _effectiveDeletedRelations();
List<DeletedEventRelation> detailRelations = const [];
if (eventName == temporaryCategoryName) {
  validLabels = getTemporaryLabels();
} else if (eventName == deletedCategoryName) {
  detailRelations = allDeletedRelations;
  validLabels = detailRelations
      .map((relation) => relation.eventName)
      .toSet();
} else if (allDeletedRelations
    .any((relation) => relation.eventName == eventName)) {
  // “全部”视图关闭合并时，单独点击某个已删除标签也要保留父子关系。
  detailRelations = allDeletedRelations
      .where((relation) => relation.eventName == eventName)
      .toList();
  validLabels = {eventName};
} else {
  validLabels = {eventName};
  for (final cat in _categories) {
    if (cat.name == eventName) {
      validLabels.addAll(cat.subCategories);
      validLabels.addAll(cat.hiddenSubCategories);
      break;
    }
  }
}
```

`getEventHistory` 的返回记录不能继续只有 `{range, label}`，需要扩展为：

```dart
typedef EventHistoryItem = ({
  String range,
  String label,
  String? parentName,
  bool isParentEvent,
});
```

当 `eventName == deletedCategoryName`，或“全部”视图关闭合并后点击某个已删除标签时，每个时间块按 `slot.categoryId + slot.label` 匹配对应的 `DeletedEventRelation`，填入当前父事件名（父事件仍存活时取当前名称，否则取删除时快照）和 `isParentEvent`。旧数据没有可靠 `categoryId` 时，只有在该 `eventName` 对应唯一删除关系的情况下才允许按标签名回退；存在多个同名关系时不得随意把时间块归到错误父事件。

因此 `deleteCategory` 现有“清理孤立引用”的逻辑需要调整：带有历史记录的槽位不能直接丢掉 `categoryId`，应保留被删除分类的 ID 作为历史来源（`findCategoryById` 找不到时不影响现有显示），清空槽位时仍按原逻辑清除。这样整体删除父事件后，时间块仍能准确对应到删除关系。

`EventDetailScreen` 只对“已删除”详情启用层级展示：按日期后再按 `parentName` 分组，父事件名作为小标题，子事件缩进显示为 `父事件 / 子事件`；`isParentEvent == true` 的记录显示为“父事件本身”。其他事件详情维持当前平铺列表。示例：

```text
3月10日
工作
  09:00 - 10:00  写代码
  14:00 - 14:30  开会
阅读（父事件本身）
  20:00 - 21:00
```

### 4. 持久化

- 本地 key `deleted_relations`（identity-scoped），使用 `SharedPreferences.setStringList` 存储每条关系的 JSON 字符串；缺失 key 视为空列表。
- `_identityScopedPreferenceBases`（`:508` 附近）加入 `deleted_relations`，保证身份命名空间迁移时不会漏掉该字段。
- 保存：`_saveData` 分类段（`:5289` `deleted_categories` 之后）追加

```dart
if (!canPersist() ||
    !await prefs.setStringList(
        _identityDataKey('deleted_relations'),
        _deletedRelations
            .map((relation) => jsonEncode(relation.toJson()))
            .toList()) ||
    !canPersist()) {
  return false;
}
```

- 加载：读取分类处（`:6288` `deleted_categories` 之后）追加，逐条 `jsonDecode` 后用 `DeletedEventRelation.fromJson` 解析；坏记录跳过并走 `_recordAppError`。
- 清空数据（`:675`）：`_deletedRelations.clear()`。

备份也属于本地数据迁移范围：

```dart
// toBackupMap()
'deletedRelations': _deletedRelations
    .map((relation) => relation.toJson())
    .toList(),

// _applyBackupMap()
final parsedDeletedRelations = <DeletedEventRelation>[];
final rawDeletedRelations = data['deletedRelations'];
if (rawDeletedRelations is List) {
  for (final raw in rawDeletedRelations) {
    if (raw is Map) {
      final relation = DeletedEventRelation.fromJson(
        Map<String, dynamic>.from(raw),
      );
      if (relation != null) parsedDeletedRelations.add(relation);
    }
  }
}
_deletedRelations
  ..clear()
  ..addAll(parsedDeletedRelations);
```

建议将 `backupVersion` 从 `1` 升为 `2`；导入 v1 备份时没有 `deletedRelations`，按空列表兼容。导入备份必须替换而不是沿用当前内存中的删除关系，避免旧身份/旧数据的墓碑污染新备份。

### 5. ProfileScreen 开关

在"全部 / 父事件"切换（`profile_screen.dart:83-103` 的 `GestureDetector`）旁边加一个同款 chip，仅"全部"模式下展示：

```dart
bool _groupDeletedEvents = true; // 内存态，与 _showParentOnly 一致不持久化
```

- 文案建议：`合并已删除` / `不合并`；`_showParentOnly == true` 时隐藏该 chip（父事件视图恒定合并）。在窄屏上使用 `Wrap` 或让控制区可换行，避免新增 chip 与现有分段控件造成横向溢出。
- `_getDataByTab`（`:570`）改为：

```dart
if (_showParentOnly) {
  return provider.getParentStatistics(start, now);
}
return provider.getStatisticsWithTemporaryGrouped(
  start, now,
  groupDeleted: _groupDeletedEvents,
);
```

- 饼图/列表颜色按索引取 `AppSemanticColors.chartAt`，新增扇区无需改 UI；点击"已删除"走 `EventDetailScreen(eventName: '已删除')` → 命中上面 `getEventHistory` 新分支。

### 6. 同步范围（本次不做）

`_deletedCategories` / `_deletedTargets` 是给远端合并用的墓碑；`_deletedRelations` **只做本地持久化**，不进分类同步文档（`CategoryDocumentMerge` / `CategorySyncDependencies`），避免改动远端文档结构。

影响：多端场景下另一端没有这份关系记录，已删除标签在那边仍显示"临时"。这是已知取舍；后续若要同步，需要给每条关系记录加时间戳并入 `CategoryDocument`，按"较新胜出"合并（同 `_deletedCategories`），会改远端格式。

本次虽然不把删除关系写进远端文档，但远端拉回的存活分类仍要在本地清理同名关系记录，或由 `_effectiveDeletedRelations()` 在读取时过滤，确保“存活优先”在多端场景下成立。

### 7. 已知取舍

- **老数据无法回填**：本方案上线前已删除的子事件/分类没有关系记录，其历史标签仍归"临时"。旧数据没有父子快照，也没有可靠 `categoryId`，无法反推，不做迁移。
- **统计按标签聚合，明细按关系展示**：同名标签在任一存活分类中出现时，历史上的同名标签也按存活关系处理；这是方案“存活优先”的明确取舍。统计扇区仍按 `eventName` 汇总，明细则按 `categoryId + eventName` 恢复父子关系；没有可靠来源时不强行猜测父事件。
- **"全部"视图口径随开关变化**：关闭合并时已删除标签按原 label 独立显示，临时标签仍合并为"临时"——这是开关的预期语义（只控制"已删除"是否聚合）。
- **备份兼容**：v1 备份没有 `deletedRelations`，导入后按空列表处理；新版本备份会保留父子关系记录。

### 8. 改动清单

| 位置 | 改动 |
| --- | --- |
| `time_provider.dart:184` / `:190` 附近 | `backupVersion` 升级为 2，并新增 `deletedCategoryName = '已删除'` |
| `lib/models/deleted_event_relation.dart` | 新增父子关系记录模型，序列化父名、事件名、事件类型、分类 ID、删除时间 |
| `:508` / `:2167` 附近 | 身份迁移 key 加入 `deleted_relations`；新增 `_deletedRelations` 及活跃/有效关系辅助方法 |
| `:4901` `addCategory` | 校验保留名；父名、可见子事件、隐藏子事件从关系记录移除 |
| `:4914` `updateCategory` | 纯改名传播并更新父名快照；按可见+隐藏并集记录真正删除的子事件；新出现标签清除关系 |
| `:4947` / `:4965` | 隐藏/恢复不记删除，直接激活时清除同名关系 |
| `:6467` `moveSubCategory` | 移动后的子事件从关系记录移除 |
| `:6539` `deleteCategory` | 防止删除临时分类；父事件本身 + 可见/隐藏子事件写入关系记录；保留历史槽位的 `categoryId` |
| `:3291` `_applyMergedCategories` | 合并后清理活跃同名关系；不改变远端文档格式 |
| `:6939` `getParentStatistics` | 按有效删除标签集合新增"已删除"分支 |
| `:6981` `getTemporaryLabels` | `known` 加入有效已删除标签 |
| `:7017` `getStatisticsWithTemporaryGrouped` | 新增 `groupDeleted` 参数，只聚合有效已删除标签 |
| `:7075` `getEventHistory` | 支持 `eventName == '已删除'`，返回父名/父子类型元数据并展开有效关系 |
| `event_detail_screen.dart` | “已删除”详情按日期 + 父事件分组，子事件缩进显示，父事件本身单独标识 |
| `:5289` / `:6288` / `:675` | 关系保存、加载、身份切换清空 |
| `:5919` / `:6021` | 备份导出/导入携带并替换 `deletedRelations` |
| `profile_screen.dart:24` / `:83` / `:570` | 新增 `_groupDeletedEvents` 开关 chip 与取数传参，并处理窄屏布局 |

## 不受影响的部分

- **词云**（`word_cloud_screen.dart`）：用 `getStatistics`，不受影响。
- **`_deletedCategories` 远端墓碑**：继续只服务于多端合并，结构与语义不变。
- **ProfileScreen 饼图/列表 UI**：颜色、点击跳转逻辑不变，只多一个扇区和一个 chip；统计数据源需要按开关切换。

## 验证方式

- `flutter analyze` 无新告警。
- 新增单测 `test/parent_statistics_deleted_test.dart`（provider 构造参照 `test/widget_test.dart`）：
  1. 分类 A 下有子事件 s，记录 1 小时 → 删除 s（`updateCategory` 提交不含 s 的列表）→ `getParentStatistics` 出现"已删除" 1 小时，A 为 0，且不含"临时"；
  2. `deleteCategory(A)` → "已删除" = A 自身 + 原子事件的总和；
  3. 重新添加同名子事件 / 新建同名分类 → 从"已删除"回到存活分支，统计只剩一份；
  4. 可见子事件移动到 `hiddenSubCategories`、以及隐藏子事件恢复时，不进入"已删除"，父事件统计仍正确；
  5. 纯改名会传播历史记录但不产生旧标签的"已删除"墓碑；真正从可见+隐藏并集中移除才会记录；
  6. `getEventHistory('已删除', tabIndex)` 返回所有有效删除关系的时间块，父事件本身和子事件都保留正确关系，且不返回后来重新激活的同名标签；
  7. `getStatisticsWithTemporaryGrouped(..., groupDeleted: false)` 时已删除标签保持独立显示，`true` 时合并为一项；
  8. 持久化往返：写入 → 重新加载 → 统计结果和父子关系一致；身份切换后不同身份的删除关系互不泄漏；
  9. 备份导出/导入会保留 `deletedRelations` 的父子字段，导入不带该字段的 v1 备份会清空旧关系；
  10. 保留名校验：临时分类不可删除，“已删除”不可作为真实分类或子事件名；
  11. 远端分类合并重新带回同名存活标签时，全部视图和已删除明细都按存活关系处理；剩余删除记录的父子关系不被破坏。
- 修改现有 `test/parent_statistics_test.dart` 中“删除父事件后，原父事件和子事件都归入临时”的旧断言，改为归入“已删除”。
- 手动（Windows）：`flutter run -d windows` → 首页用"临时"对话框创建若干事件 → 个人中心 → "全部"模式下有"已删除"chip 可切换；切到"父事件" → 饼图显示"临时"与"已删除"两个聚合项，点击"已删除"可看明细；编辑事件删除一个有过记录的子事件 / 整体删除一个有过记录的事件 → 时长进入"已删除"，不散落在"临时"里，并在明细中按“父事件 → 子事件”显示，整体删除时同时显示父事件本身与所有可见/隐藏子事件。

## 下一阶段：真正恢复操作（回收站）

> 本节是下一阶段的恢复功能方案，不属于当前“已删除统计与关系明细”提交。当前 DeletedEventRelation 只负责展示历史父子关系，不能直接恢复完整分类。

### 1. 恢复目标

恢复成功后，分类重新出现在首页，历史时间块重新按原分类统计，并尽量恢复删除前的颜色、顺序、可见/隐藏子事件和关联目标。

恢复必须使用原来的 categoryId，不能新建一个分类 ID。当前删除流程已经保留历史 TimeSlot.categoryId，所以分类恢复后历史槽位会自动重新归属，不需要批量改写历史记录。

恢复原则：

1. 不静默覆盖当前存活的分类、子事件或目标。
2. 所有冲突先检查；存在冲突时整个恢复操作不执行，避免恢复一半。
3. 本地保存成功后才移除恢复快照；保存失败时快照保留，允许重试。
4. 远端较新的删除不会被后台同步自动抵消；只有用户明确确认后才可以强制恢复。
5. 当前提交之前删除但没有完整快照的数据，仍只能通过删除前备份恢复。

### 2. 恢复快照模型

新增三个本地模型，和 DeletedEventRelation 分开：

~~~dart
class DeletedCategorySnapshot {
  final String snapshotId; // categoryId + deletion sequence
  final Category category; // 删除瞬间的完整分类
  final int originalCategoryIndex;
  final List<DeletedTargetSnapshot> targets;
  final int deletedAt;
}

class DeletedSubCategorySnapshot {
  final String snapshotId;
  final String categoryId;
  final String parentName;
  final String eventName;
  final int originalIndex;
  final bool wasHidden;
  final int deletedAt;
}

class DeletedTargetSnapshot {
  final Target target;
  final int originalIndex;
}
~~~

保存规则：

- 父事件快照保存完整 Category，包括 ID、名称、颜色、可见子事件、隐藏子事件和原 updatedAt。
- 父事件删除时同时保存该分类被级联删除的目标及目标原位置，因为当前 deleteCategory 会移除这些目标。
- 子事件快照保存父分类 ID、事件名、原位置和原来是否隐藏。
- snapshotId 不能只用事件名；同一分类删除、恢复、再次删除必须能区分不同删除批次。
- 父事件整体删除时，只创建一条父事件快照；当时仍存在的子事件随 Category 一起恢复。
- 如果子事件在父事件删除之前已被单独删除，父快照不应把它重新加入；它自己的子事件快照仍需单独恢复。

三类数据的职责：

~~~text
DeletedEventRelation       → 已删除历史明细展示
DeletedCategorySnapshot    → 恢复整个父事件
DeletedSubCategorySnapshot → 恢复一个已删除子事件
~~~

### 3. 删除时记录快照

updateCategory 发现子事件从可见/隐藏子事件并集中真正消失时：

1. 从旧分类找到它所在列表和索引。
2. 写入 DeletedSubCategorySnapshot。
3. 继续写入现有 DeletedEventRelation。
4. 再执行分类更新。

纯改名、可见与隐藏之间移动、重新添加同名子事件都不产生新的删除快照。

deleteCategory 移除分类前：

1. 复制完整 Category，并记录分类原索引。
2. 找出所有 categoryId 相同的目标，并记录目标原索引。
3. 写入 DeletedCategorySnapshot。
4. 写入现有关系记录和分类删除墓碑。
5. 移除分类和目标，但继续保留历史槽位的 categoryId。

### 4. Provider 恢复接口

增加只读快照列表和异步恢复结果：

~~~dart
List<DeletedCategorySnapshot> get deletedCategorySnapshots;
List<DeletedSubCategorySnapshot> get deletedSubCategorySnapshots;

Future<DeletedRestoreResult> restoreDeletedCategory(String snapshotId);
Future<DeletedRestoreResult> restoreDeletedSubCategory(String snapshotId);
~~~

DeletedRestoreResult 至少包含：

~~~text
restored              恢复成功
alreadyRestored       已经被其他设备或此前操作恢复
snapshotNotFound      没有可恢复快照
parentNotRestored     父事件尚未恢复
conflict              当前数据存在冲突
remoteDeleteConflict  远端有更新的删除操作
saveFailed            本地保存失败，快照保留
~~~

### 5. 恢复流程

#### 5.1 恢复父事件

1. 禁止在远程只读视图中操作，并防止同一条记录重复点击。
2. 根据 snapshotId 找到快照，确认当前没有相同 categoryId 的存活分类。
3. 检查分类名、所有子事件名和所有目标 ID是否冲突。
4. 按 originalCategoryIndex 插回分类；超出范围时插到末尾。
5. 按原索引恢复目标，并把目标 updatedAt 设置为大于删除墓碑。
6. 把分类 updatedAt 设置为大于 deletedCategories[categoryId]。
7. 只清除这份快照中包含的父/子关系；同分类下更早单独删除的子事件关系必须保留。
8. 保存成功后清除本地删除墓碑、关系和恢复快照，刷新统计并发起同步。
9. 保存失败时回滚内存状态，保留快照。

恢复父事件不会偷偷恢复此前已经单独删除的子事件；用户需要再点击对应的“恢复子事件”。

#### 5.2 恢复子事件

1. 用 categoryId 找到当前父分类；找不到时返回 parentNotRestored。
2. 检查事件名是否已经被任意存活标签占用。
3. 按 wasHidden 插回原来的可见或隐藏列表，位置使用 originalIndex。
4. 更新父分类 updatedAt，清除对应的关系和子事件快照。
5. 保存成功后刷新统计；历史时间块无需改写，会自动回到父事件。

恢复子事件只修改父分类的子事件集合，不覆盖父分类颜色、名称或其他子事件。

### 6. 冲突处理

默认不自动合并，具体规则如下：

| 情况 | 处理 |
| --- | --- |
| 相同 categoryId 已存在且内容完全一致 | 视为已经恢复，清理本地快照，不重复插入 |
| 相同 categoryId 已存在但内容不同 | 返回 conflict，不覆盖当前分类 |
| 分类名与其他存活父/子事件重名 | 阻止恢复，用户先处理当前同名事件 |
| 子事件名与任意存活标签重名 | 阻止恢复；当前统计按标签名聚合，自动恢复会造成歧义 |
| 名称是“临时”或“已删除” | 阻止恢复 |
| 父分类不存在 | 子事件不能单独恢复，先恢复父事件 |
| 目标 ID 已存在且内容一致 | 视为目标已经恢复，不重复添加 |
| 目标 ID 存在但内容不同 | 整个父事件恢复中止，不只恢复一部分 |
| 原分类/子事件位置已经变化 | 按原索引插入，其他项目顺延，不覆盖现有项目 |

不自动提供“改名恢复”。如果恢复成新名称，需要同步迁移历史 label，属于另一个“恢复为副本”功能。

用户手动重新创建同名事件时，恢复快照不能被静默删除；统计可以按存活标签处理，但回收站应显示“同名事件已存在，无法直接恢复”，待用户解决冲突后再恢复原快照。

### 7. 远端同步规则

恢复不是简单删除本地墓碑。分类和目标必须使用原 ID，并设置严格大于删除时间的更新时间：

~~~text
restoredCategory.updatedAt > deletedCategories[categoryId]
restoredTarget.updatedAt > deletedTargets[targetId]
~~~

现有合并规则是“删除时间大于更新时间才删除”，因此恢复后的新版本可以压过旧墓碑。远端较旧的删除墓碑即使暂时保留，也不会阻止这次恢复。

恢复前应尽量强制拉取分类和目标：

- 远端已有相同恢复结果：返回 alreadyRestored，跟随远端状态。
- 远端同 ID 但内容不同：返回 conflict，不覆盖。
- 远端删除时间晚于本地删除快照：默认返回 remoteDeleteConflict，不自动复活。
- 用户确认“以本次恢复为准”后，才允许用大于远端删除时间的 restoreAt 再推送。

第一版恢复快照继续使用 identity-scoped 本地存储并写入备份；恢复后的分类和目标通过现有同步传播。其他设备没有本地快照时不能直接打开该事件的恢复入口。若以后要求任意设备都能恢复，需要把快照和 restoredAt 一起加入远端文档，并设计“已恢复”状态，不能只同步快照列表。

### 8. 回收站 UI

当前 EventDetailScreen 继续负责历史时间段展示，新增 DeletedEventArchiveScreen 负责可恢复项目：

~~~text
已删除事件

父事件
工作                         删除于 9月10日   [恢复父事件]
  删除时包含：写代码、开会、（隐藏）复盘

已删除子事件
专项工作 / 写作               删除于 9月11日   [恢复子事件]
~~~

从“已删除”明细的 AppBar 或操作菜单进入回收站。恢复成功后条目消失，统计从“已删除”回到原父事件；冲突条目保留并显示具体原因。没有完整快照的旧关系只显示历史，不显示恢复按钮，并提示“仅支持从备份恢复”。

### 9. 持久化、失败恢复与测试

- 新增 identity-scoped key：deleted_category_snapshots、deleted_subcategory_snapshots。
- 备份新增 deletedCategorySnapshots、deletedSubCategorySnapshots，backupVersion 从 2 升为 3；v1/v2 导入时快照为空。
- 身份切换和全量导入必须替换快照，不能追加其他身份的数据。
- 恢复期间设置 _isRestoringDeletedEvent，禁用同一条按钮和相关同步竞争。
- 保存成功后才移除快照；保存失败保留快照并回滚内存状态。
- 如 SharedPreferences 分段写入无法保证分类、目标、快照一致，增加 deleted_restore_journal，在启动时完成提交或回滚。
- 测试覆盖：父/子事件恢复、可见/隐藏位置、目标恢复、重复恢复、同 ID 冲突、同名冲突、父分类缺失、远端新删除、保存失败、备份和身份隔离。

### 10. 实施顺序

1. 新增快照模型和序列化测试。
2. 在 updateCategory、deleteCategory 捕获快照，补本地存储和备份。
3. 在 TimeProvider 实现冲突检查、父事件恢复和子事件恢复。
4. 增加失败回滚/恢复日志和远端时间戳处理。
5. 新增 DeletedEventArchiveScreen 和恢复确认/冲突提示。
6. 完成定向测试、完整测试和手动验证后，作为独立提交合入。
