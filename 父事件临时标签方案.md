# 父事件临时标签方案

## 背景与问题

个人中心（ProfileScreen）的"父事件"视图统计来自 `TimeProvider.getParentStatistics`（`lib/providers/time_provider.dart:3198`）。当前逻辑：

- label 是某分类 `subCategories` 中的子事件 → 并入该父事件
- 否则（非子事件）→ **作为独立项直接显示**

导致：通过首页"临时"对话框创建的临时事件（label 为任意输入名、categoryId 不在 `_categories` 中）各自成为独立的"父事件"扇区，污染父事件饼图。

## 目标行为

1. **隐藏的子事件（`hiddenSubCategories`）也累计到其原父事件名下**。
2. label 既不是任何分类的子事件（含隐藏），也不是任何分类的 name → **统一归入"临时"聚合项**，不再独立显示。
3. 点击父事件列表中的"临时"聚合项，可查看所有临时事件的明细。

## 改动方案

以下改动仅作方案参考，尚未实施。

### 1. `lib/providers/time_provider.dart` — `getParentStatistics`（约 3198-3224 行）

```dart
Map<String, double> getParentStatistics(DateTime start, DateTime end) {
  final detailStats = getStatistics(start, end);
  Map<String, double> parentStats = {};

  // 子事件（含隐藏子事件）→ 父事件名映射；真正的父事件名集合
  final Map<String, String> childToParent = {};
  final Set<String> parentNames = {};
  for (final cat in _categories) {
    parentNames.add(cat.name);
    for (final sub in cat.subCategories) {
      childToParent[sub] = cat.name;
    }
    for (final sub in cat.hiddenSubCategories) {
      childToParent[sub] = cat.name;
    }
  }

  detailStats.forEach((label, hours) {
    final parentName = childToParent[label];
    if (parentName != null) {
      parentStats[parentName] = (parentStats[parentName] ?? 0) + hours;
    } else if (parentNames.contains(label)) {
      parentStats[label] = (parentStats[label] ?? 0) + hours;
    } else {
      // 临时事件（无归属）：统一归入"临时"
      parentStats['临时'] = (parentStats['临时'] ?? 0) + hours;
    }
  });

  return parentStats;
}
```

### 2. `lib/providers/time_provider.dart` — 新增辅助方法 `getTemporaryLabels()`

```dart
/// 收集所有不属于任何分类（父事件/子事件）的历史标签（即临时事件）
Set<String> getTemporaryLabels() {
  final known = <String>{};
  for (final cat in _categories) {
    known.add(cat.name);
    known.addAll(cat.subCategories);
    known.addAll(cat.hiddenSubCategories);
  }
  final temps = <String>{};
  for (final slots in _dailySlots.values) {
    for (final slot in slots) {
      if (slot.recorded && slot.label != null && !known.contains(slot.label)) {
        temps.add(slot.label!);
      }
    }
  }
  return temps;
}
```

### 3. `lib/providers/time_provider.dart` — `getEventHistory`（约 3269-3310 行）

将：

```dart
Set<String> validLabels = {eventName};
for (final cat in _categories) {
  if (cat.name == eventName) {
    validLabels.addAll(cat.subCategories);
    validLabels.addAll(cat.hiddenSubCategories);
    break;
  }
}
```

改为：

```dart
Set<String> validLabels;
if (eventName == '临时') {
  validLabels = getTemporaryLabels();
} else {
  validLabels = {eventName};
  for (final cat in _categories) {
    if (cat.name == eventName) {
      validLabels.addAll(cat.subCategories);
      validLabels.addAll(cat.hiddenSubCategories);
      break;
    }
  }
}
```

## 不受影响的部分

- **"全部"视图**（`getStatistics`）：仍显示所有 label 各自独立，符合"全部"语义。
- **词云**（`word_cloud_screen.dart`）：使用 `getStatistics`，不受影响。
- **ProfileScreen UI**（`profile_screen.dart`）：列表/饼图颜色用 `Colors.primaries`，无需改动；点击"临时"项经 `EventDetailScreen(eventName: '临时')` → 新 `getEventHistory` 逻辑可正常展示明细。

## 验证方式

- `flutter analyze` 无新告警。
- 手动（Windows）：`flutter run -d windows`，首页用"临时"对话框创建若干事件 → 个人中心 → 切到"父事件" → 饼图/列表显示"临时"聚合项，点击可看明细；被隐藏的子事件计入原父事件。
