# 那年今日 (On This Day) 功能实现计划

## 需求

- 打开 App 时弹出"那年今日"窗口，展示往年同一天的数据
- **每天最多弹出一次**：当天第一次打开 App 时弹出，当天之后再次打开不再弹出；第二天打开时再次弹出
- 用 SharedPreferences 持久化记录"上次弹出日期"，实现跨会话的每日一次限制
- 展示内容：**时间记录 + 日记 + 出行记录**

## 涉及文件

| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `lib/models/on_this_day_entry.dart` | 数据模型 |
| 新建 | `lib/services/on_this_day_service.dart` | 三数据源收集编排（时间记录/日记/出行） |
| 新建 | `lib/widgets/on_this_day_sheet.dart` | 底部弹出组件 |
| 修改 | `lib/screens/main_screen.dart` | 触发弹出逻辑 + 每日一次判断 |
| 修改 | `lib/providers/time_provider.dart` | 新增时间记录查询方法 + 数据就绪标志 |

---

## 1. 数据模型 `lib/models/on_this_day_entry.dart`

```dart
/// 一年前的今天发生了什么
class OnThisDayEntry {
  final int year;                              // 年份，如 2025
  final int totalMinutes;                      // 当天时间记录总时长（分钟）
  final List<ActivitySummary> activities;       // 活动列表（按时长降序）

  // 日记（可能只有乖乖的、只有晶晶的、或两人都有）
  // 注意：以下字段可变，由 OnThisDayService 收集后填充
  String? diaryG;                              // 乖乖那天的日记（摘要）
  String? diaryJ;                              // 晶晶那天的日记（摘要）

  // 出行
  String? travelLocation;                      // 那天去了哪
  String? travelEvent;                         // 那天做了什么

  bool get hasDiary => diaryG != null || diaryJ != null;
  bool get hasTravel => travelLocation != null;

  String get yearsAgoLabel {
    final diff = DateTime.now().year - year;
    if (diff == 1) return '去年';
    return '${diff}年前';
  }
}

/// 单项活动时长
class ActivitySummary {
  final String label;    // 活动名称
  final int minutes;     // 分钟数
}
```

---

## 2. 数据提取与处理（三种数据源）

**数据流总览**：
```
TimeProvider.getOnThisDayEntries()  → 时间记录（同步，主数据源）
OnThisDayService.enrichEntries()    → 补全日记 + 出行（异步，增强内容）
```

### 2.1 时间记录 —— 同步，O(1) 内存查询

**数据源**：`TimeProvider._dailySlots`（`Map<String, List<TimeSlot>>`，键 `yyyy-MM-dd`，**全量历史数据都在内存**）。

**先加数据就绪标志**（解决时序问题）：`TimeProvider._init()` 是 fire-and-forget 异步加载，弹窗查询必须等数据加载完成才能准确。在 `_init()` 中 `_loadData()` 之后标记就绪：

```dart
// time_provider.dart
bool _isDataLoaded = false;
bool get isDataLoaded => _isDataLoaded;

Future<void> _init() async {
  try {
    await _loadData();
  } finally {
    _isDataLoaded = true;   // 无论加载成败都标记就绪，避免弹窗被静默跳过
  }
  notifyListeners();
  ...
}
```

**在 `TimeProvider` 中新增方法**（插入位置：`getStatistics` 附近）：

```dart
/// 查询往年今日的时间记录，返回有数据的年份列表
List<OnThisDayEntry> getOnThisDayEntries() {
  final now = DateTime.now();
  final month = now.month;
  final day = now.day;
  final currentYear = now.year;
  final entries = <OnThisDayEntry>[];

  // 从去年开始往前查，最多5年
  for (int year = currentYear - 1; year >= currentYear - 5; year--) {
    if (year < 2020) break;
    final dateKey = '$year-${month.toString().padLeft(2, '0')}-${day.toString().padLeft(2, '0')}';
    final slots = _dailySlots[dateKey];
    if (slots == null || slots.isEmpty) continue;

    final stats = <String, int>{}; // label -> 10分钟槽位数
    for (final slot in slots) {
      if (slot.recorded && slot.label != null && slot.label!.isNotEmpty) {
        stats[slot.label!] = (stats[slot.label!] ?? 0) + 1;
      }
    }

    if (stats.isEmpty) continue;

    final activities = stats.entries
        .map((e) => ActivitySummary(label: e.key, minutes: e.value * 10))
        .toList()
      ..sort((a, b) => b.minutes.compareTo(a.minutes));

    final totalSlots = stats.values.fold<int>(0, (sum, c) => sum + c);
    entries.add(OnThisDayEntry(
      year: year,
      totalMinutes: totalSlots * 10,
      activities: activities,
    ));
  }

  return entries;
}
```

**处理方式**：每个槽位 = 10 分钟，按 label 聚合计数 → 转分钟 → 按时长降序。返回的 `entries` 只含时间记录，日记/出行字段为 null，稍后由 2.2/2.3 补全。

### 2.2 日记 —— 优先同步缓存，兜底本地草稿

**数据源**（两个可选通道）：

| 通道 | 方法 | 同步/异步 | 说明 |
|------|------|-----------|------|
| A. 搜索缓存（优先） | `DiarySearchService.getCachedContent(kind.code, date)` | 同步 | 内存缓存，key 格式 `g_yyyy-MM-dd`；App 启动后台索引全部日记 |
| B. 本地草稿（兜底） | `DiaryLocalStore.loadDraftBody(kind, date)` | 异步 | SharedPreferences，key `diary_draft_g_yyyymmdd`；每次读取快 |

**处理方式**：
- 每个年份查两种日记：`DiaryKind.g`（乖乖）、`DiaryKind.j`（晶晶）
- 优先同步读缓存 A，未命中再异步读草稿 B
- 日记正文可能很长，**只取开头摘要**（如前 80 字符），存到 `entry.diaryG` / `entry.diaryJ`

### 2.3 出行记录 —— 读整份文档，解析一次复用

**数据源**：`TravelLocalStore.loadDraft()` 返回整份 markdown（含所有年份的记录），`TravelRecordsDocument.fromMarkdown(markdown)` 解析。

**处理方式**：
- **只读取并解析一次**整份出行文档（避免每个年份都重复解析）
- 解析后按日期键 `yyyy-MM-dd` 匹配每个年份的出行记录
- 补全 `entry.travelLocation` / `entry.travelEvent`

### 2.4 编排入口 —— `OnThisDayService`（新建 `lib/services/on_this_day_service.dart`）

把三种数据源的收集逻辑封装成静态工具类，MainScreen 只调一行。遵循项目现有 30 个 Service 的架构习惯：

```dart
/// 收集往年今日的所有数据（时间记录 + 日记 + 出行）
class OnThisDayService {
  static const int maxYears = 5;            // 往前查几年
  static const int maxSummaryChars = 80;    // 日记摘要截取长度

  /// 收集三数据源，返回完整数据的列表（按年份从近到远）
  /// [entries] 由 TimeProvider.getOnThisDayEntries() 提供（仅含时间记录）
  static Future<List<OnThisDayEntry>> enrichEntries(
    TimeProvider provider,
    List<OnThisDayEntry> entries,
  ) async {
    final now = DateTime.now();

    // ① 补全日记（异步，逐年）
    for (final entry in entries) {
      final date = DateTime(entry.year, now.month, now.day);
      final g = await _readDiary(DiaryKind.g, date);
      final j = await _readDiary(DiaryKind.j, date);
      entry.diaryG = g;
      entry.diaryJ = j;
    }

    // ② 补全出行（异步，整份解析一次复用）
    final travelMap = await _loadTravelMap();
    for (final entry in entries) {
      final dateKey = dateKeyOf(DateTime(entry.year, now.month, now.day));
      final travel = travelMap[dateKey];
      if (travel != null) {
        entry.travelLocation = travel.location;
        entry.travelEvent = travel.event;
      }
    }

    return entries;
  }

  static Future<String?> _readDiary(DiaryKind kind, DateTime date) async {
    // 通道 A：搜索缓存（同步）
    if (DiarySearchService.isLoaded) {
      final cached = DiarySearchService.getCachedContent(kind.code, date);
      if (cached != null && cached.trim().isNotEmpty) {
        return _summarize(cached);
      }
    }
    // 通道 B：本地草稿（异步）
    final draft = await DiaryLocalStore.loadDraftBody(kind, date);
    if (draft != null && draft.trim().isNotEmpty) return _summarize(draft);
    return null;
  }

  /// 剥离 front matter + 空行，取前 N 字作为摘要
  /// 复用项目现有 _extractBodyFromMarkdown 的同款正则（diary_screen.dart:395）
  static String? _summarize(String content) {
    final match = RegExp(r'^---\n([\s\S]*?)\n---\n?').firstMatch(content);
    var text = (match == null ? content : content.substring(match.end)).trim();
    if (text.isEmpty) return null;
    if (text.length <= maxSummaryChars) return text;
    return '${text.substring(0, maxSummaryChars)}…';
  }

  /// 读取整份出行记录文档，返回按日期键索引的 map
  static Future<Map<String, TravelRecord>> _loadTravelMap() async {
    final markdown = await TravelLocalStore.loadDraft();
    if (markdown == null || markdown.trim().isEmpty) return {};
    try {
      final doc = TravelRecordsDocument.fromMarkdown(markdown);
      return {for (final r in doc.records) r.dateKey: r};
    } catch (_) {
      return {};
    }
  }

  /// 统一日期键格式 yyyy-MM-dd（与 TimeProvider._getDateKey 一致）
  static String dateKeyOf(DateTime date) {
    return '${date.year}-${date.month.toString().padLeft(2, '0')}-${date.day.toString().padLeft(2, '0')}';
  }
}
```

---

## 3. 底部弹出组件 `lib/widgets/on_this_day_sheet.dart`

### 设计要点

- 使用 `showModalBottomSheet`，风格与现有 AI 对话弹窗一致
- 圆角顶部 + 拖拽手柄
- 品牌绿色作为强调色
- 卡片式展示每年数据

### 入口函数

```dart
Future<void> showOnThisDaySheet(BuildContext context, {
  required List<OnThisDayEntry> entries,
  required int month,
  required int day,
}) {
  return showModalBottomSheet(
    context: context,
    isScrollControlled: true,
    shape: const RoundedRectangleBorder(
      borderRadius: BorderRadius.vertical(top: Radius.circular(16)),
    ),
    showDragHandle: true,
    builder: (ctx) => _OnThisDayContent(entries: entries, month: month, day: day),
  );
}
```

### 内容组件 `_OnThisDayContent`

**有数据时，每年一张卡片：**

```
┌──────────────────────────────────┐
│         ── 拖拽手柄 ──            │
│                                  │
│       📅 那年今日 · 8月1日        │
│                                  │
│  ┌──────────────────────────┐    │
│  │  2024年 · 2年前           │    │
│  │  ─────────────────────    │    │
│  │  🗓 学习 3h20m · 运动 1h   │    │   ← 时间记录
│  │  ✈️ 去了「西湖」玩         │    │   ← 出行（有则显示）
│  │  📖 乖乖: "今天去西湖散步，  │    │   ← 日记摘要（有则显示）
│  │     天特别蓝……"            │    │
│  └──────────────────────────┘    │
│                                  │
│  ┌──────────────────────────┐    │
│  │  2023年 · 3年前           │    │
│  │  ─────────────────────    │    │
│  │  📖 晶晶: "加班到很晚……"   │    │
│  └──────────────────────────┘    │
│                                  │
│  没有更多记录了                   │
└──────────────────────────────────┘
```

**无数据时：**
- 简洁提示"这天的记忆还在积累中~"

---

## 4. MainScreen 触发逻辑 `lib/screens/main_screen.dart`

### 设计思路

用一个 SharedPreferences 键 `on_this_day_last_shown_date` 存储上次弹出那天的日期（格式 `yyyy-MM-dd`）。

每次启动 App 时：
1. 读取该键，如果等于今天 → 今天已弹过，跳过
2. 不等于今天 → 异步收集数据（时间记录 + 日记 + 出行），有数据才弹出，弹出后写入今天的日期
3. 内存中再放一个 `_onThisDayShown` 会话级标志，防止同一会话内被多次触发（如页面重建）

### 修改点

在 `_MainScreenState` 中：

```dart
bool _onThisDayShown = false;      // 已弹出（会话级）
bool _onThisDayChecking = false;   // 正在检查中（防并发重复触发）
static const _lastShownDateKey = 'on_this_day_last_shown_date';

@override
void initState() {
  super.initState();
  WidgetsBinding.instance.addObserver(this);

  // 监听 TimeProvider：数据就绪后再触发弹出（避免查询到空数据）
  final provider = context.read<TimeProvider>();
  provider.addListener(_tryShowOnThisDay);

  // 第一帧后也尝试一次（此时数据可能已就绪）
  WidgetsBinding.instance.addPostFrameCallback((_) {
    _tryShowOnThisDay();
  });
}

@override
void dispose() {
  context.read<TimeProvider>().removeListener(_tryShowOnThisDay);
  WidgetsBinding.instance.removeObserver(this);
  super.dispose();
}

Future<void> _tryShowOnThisDay() async {
  // 已弹过 / 正在检查中 → 跳过（防并发：notifyListeners 多次触发时防止弹两次）
  if (_onThisDayShown || _onThisDayChecking) return;
  if (!mounted) return;
  _onThisDayChecking = true;
  try {
    final provider = context.read<TimeProvider>();
    if (!provider.isDataLoaded) return;  // 数据未就绪，等 notifyListeners 再触发

    final now = DateTime.now();
    final todayKey = OnThisDayService.dateKeyOf(now);  // 复用 Service 的日期键工具

    // 今天已经弹过就不再弹（跨会话持久化判断）
    final prefs = await SharedPreferences.getInstance();
    if (prefs.getString(_lastShownDateKey) == todayKey) return;

    // ① 时间记录（同步）
    final entries = provider.getOnThisDayEntries();
    if (entries.isEmpty) return;   // 没有往年时间记录就不弹

    // ② 日记 + 出行 补全（异步，交给 Service）
    await OnThisDayService.enrichEntries(provider, entries);

    // 本次会话已弹过，避免重复触发
    _onThisDayShown = true;
    await prefs.setString(_lastShownDateKey, todayKey);

    // 稍微延迟，让 UI 先稳定
    Future.delayed(const Duration(milliseconds: 600), () {
      if (!mounted) return;
      showOnThisDaySheet(
        context,
        entries: entries,
        month: now.month,
        day: now.day,
      );
    });
  } finally {
    _onThisDayChecking = false;
  }
}
```

**新增 import**（MainScreen 只需 3 个，其余逻辑都在 Service 内部）：
- `package:shared_preferences/shared_preferences.dart`
- `package:time_manager/services/on_this_day_service.dart`
- `package:time_manager/widgets/on_this_day_sheet.dart`

### 关键设计

- **跨会话持久化**：`_lastShownDateKey` 存上次弹出日期，实现"每天最多一次"，App 重启、杀进程后依然生效
- **会话级标志**：`_onThisDayShown` 防止同一会话内因 rebuild 等原因重复触发
- **数据就绪时序**：`TimeProvider.isDataLoaded` 保证查询发生在数据加载完成后；MainScreen 通过 `addListener` 监听，数据就绪的 `notifyListeners()` 会再次触发查询，确保当天第一次打开一定弹得出
- **无数据不打扰**：往年同日没有时间记录就不弹，也不标记日期（这样即使稍后数据同步/导入进来，当天仍有机会弹出）
- **日记/出行只是增强**：只有时间记录时也弹，日记和出行异步补全；都没有也不影响
- **异步延迟**确保首屏 UI 先渲染完成

---

## 5. 性能评估

| 数据源 | 查询方式 | 最大开销 | 结论 |
|--------|----------|----------|------|
| 时间记录 | 内存 Map O(1) × 5年 | 5 次查找 | 极快 |
| 日记 | 内存缓存同步读；未命中才读 SharedPreferences | 5年×2人=10次 SP 读取 | 毫秒级 |
| 出行 | 整份文档解析 1 次 + Map 查找 | 1 次 JSON 解析 | 毫秒级 |

最坏情况总耗时在几十毫秒内，不影响启动速度。真正的风险是时序（数据未加载完就查询到空结果），已通过 `isDataLoaded` 标志 + Provider 监听解决。

---

## 6. 实现顺序

1. 创建 `lib/models/on_this_day_entry.dart`
2. 在 `TimeProvider` 添加 `isDataLoaded` 标志 + `getOnThisDayEntries()` 方法（时间记录提取）
3. 创建 `lib/services/on_this_day_service.dart`（日记/出行收集编排）
4. 创建 `lib/widgets/on_this_day_sheet.dart`（弹窗组件）
5. 修改 `lib/screens/main_screen.dart` 添加触发逻辑 + 每日一次持久化判断
6. 创建 `test/on_this_day_test.dart`（核心逻辑测试）

---

## 7. 测试计划 `test/on_this_day_test.dart`

项目现有测试仅 1 个 smoke test，本次为新增的核心逻辑补充单元测试。

### 7.1 `OnThisDayService.summarizeDiary` 测试（纯函数，最易测）

为便于测试，`_summarize` 提升为**公开静态方法** `summarizeDiary`（同时保留 `_readDiary` 内部调用）：

| 用例 | 输入 | 预期 |
|------|------|------|
| 有 front matter | `---\ntitle: x\ndate: y\n---\n\n正文内容` | `正文内容`（剥离头） |
| 正文超 80 字 | 100 字正文 | 前 80 字 + `…` |
| 空内容 | `''` / `'---\ntitle:x\n---'` | `null` |
| 无 front matter | `纯正文` | `纯正文`（原样） |

### 7.2 `TimeProvider.getOnThisDayEntries` 测试

用 `importBackupJson` 注入 `dailySlots` 数据后验证：

**注入数据格式**（`importBackupJson` 需要 `categories` + `dailySlots`）：

```dart
final provider = TimeProvider();
await provider.importBackupJson(jsonEncode({
  'categories': [{'id': 'c1', 'name': '学习', 'color': 0xFF000000, 'subCategories': [], 'hiddenSubCategories': []}],
  'targets': [],
  'dailySlots': {
    '2025-08-01': [/* 若干 recorded slot，label=学习 */],
    '2024-08-01': [/* 若干 recorded slot，label=运动 */],
    // 2024-08-02 不应出现（日期不匹配）
    '2024-08-02': [/* 若干 recorded slot */],
    '2020-08-01': [/* 超过 5 年上限，不应返回 */],
    '2025-08-03': [],
  },
}));
```

| 用例 | 预期 |
|------|------|
| 返回年份 | 只含 `2024`、`2025`（今年 2026 则往前5年=2021起，2020 超限） |
| 排序 | 从近到远（2025 在前） |
| 聚合 | 同 label 槽位合并，分钟数 = 槽位数 × 10 |
| 时长降序 | activities 按时长降序 |
| 跳空 | 无记录日期 / 空槽位日期不产生 entry |

> 注意：`TimeProvider` 构造会触发 `_init()` 异步加载 SharedPreferences，测试中需 `SharedPreferences.setMockInitialValues({})` 或等待 `_init()` 完成后再断言。

### 7.3 运行命令

```bash
flutter test test/on_this_day_test.dart
```

---

## GSTACK REVIEW REPORT

### Runs

| 轮次 | 工具 | 结果 |
|------|------|------|
| Step 0 范围挑战 | 代码探索（TimeProvider / DiaryScreen / DiarySearchService / TravelLocalStore） | 通过：4 文件最小集，未触发复杂度 gate |
| Section 1 架构 | MainScreen / TimeProvider._init 读取 | 通过 |
| Section 2 代码质量 | diary_screen 正则 / diary_search_service key 格式 | 通过 |
| Section 3 测试 | widget_test / importBackupJson 注入路径 | 通过 |
| Section 4 性能 | 三数据源查询路径 | 通过 |

### Status

**READY**（评审后已修订计划）

### Findings（共 7 项，全部已解决）

| # | 严重度 | 问题 | 决议 |
|---|--------|------|------|
| A1 | 高 | 模型 `final String? diaryG` 被 `entry.diaryG = ...` 赋值 → 编译错误 | 改为可变字段 |
| A2 | 中 | 数据收集逻辑塞进纯导航壳 MainScreen，职责漂移 | 抽到新建 `OnThisDayService` |
| A4 | 低 | `_loadData()` 异常时 `isDataLoaded` 永不置位 → 当天静默跳过 | `try-finally` 兜底 |
| C1 | 中 | `_dateKeyOf` 与项目多处日期格式化重复 | 收敛到 `OnThisDayService.dateKeyOf` 复用 |
| C2 | 低 | 魔法数字（80字/5年/600ms）散落 | 提取为 `OnThisDayService` 常量 |
| C3 | 中 | `_summarize` 正则与项目现有 `_extractBodyFromMarkdown` 不一致 | 复用同款正则 |
| R1 | 中 | `addListener` 多次触发 + `await` 并发 → 可能弹两次窗 | 加 `_onThisDayChecking` 防并发标志 |

### 新增范围

- `lib/services/on_this_day_service.dart`（评审新增）
- `test/on_this_day_test.dart`（评审新增，覆盖 `summarizeDiary` + `getOnThisDayEntries`）

### VERDICT

**APPROVED** — 计划范围合理（5 文件 + 1 测试），架构与现有 Service 分层一致，性能无风险，7 项评审发现已全部修订。可进入实现。

NO UNRESOLVED DECISIONS

---

## 8. 实现后复审修订记录（第二轮回溯）

实现完成后收到独立审查，逐条验证并修复。其中 1 项审查结论有误（正则测试），其余全部属实并已修复。

### 功能缺口修复

| 问题 | 修复 |
|------|------|
| **[高] 只有日记/出行的年份不展示** | `OnThisDayService.collectEntries()` 重写：三数据源合并，任一命中即创建 entry（`putIfAbsent`）。触发条件改为"任一有数据就弹"（用户确认） |
| **[高] 首次启动漏掉已同步日记** | MainScreen `initState` 主动 `_loadDiaryIndexInBackground()`（需 token）；`_tryShowOnThisDay` 弹窗前 `_waitForDiaryIndex()` 等待索引（最多15秒，超时继续） |
| **[中] 弹窗前就标记已显示** | `setString` 移到弹窗 `onShown` 回调（真正显示后落盘） |
| **[中] `_loadData()` 失败时 notifyListeners 不执行** | `_init()` 改为 `try-catch-finally`，catch 后仍 `notifyListeners()` |
| **[中] 跨午夜不弹** | `_onThisDayShown` 改为 `_onThisDayShownDateKey`（记录日期）；`resumed` 时重新检查 |

### 代码味道清理

| 问题 | 修复 |
|------|------|
| `OnThisDayService.maxYears` 未使用，Provider 硬编码 5 | 年份枚举移入 `collectEntries`，使用 `maxYears` 常量 |
| `enrichEntries` 的 provider 参数无用 | 方法重写为 `collectEntries(provider)`，参数真实使用 |
| 日期键格式化重复 | 统一用 `OnThisDayService.dateKeyOf` |
| `isDataLoaded` 名称不准确 | 改名 `isInitialLoadFinished` |
| `diaryG/diaryJ` 可读性弱 | 改名 `diaryGuaiGuai` / `diaryJingJing` |
| 两套时长格式化 | 合并为 `_formatDuration` / `_formatDurationCompact` |

### 审查误判

- **正则测试会失败**：审查称 `'---\ntitle: x\n---'` 不会返回 null。实际运行验证 `\n?` 为可选换行，能正确匹配，body 为空 → 返回 null。测试实际通过。
- **CRLF 不支持**：属实，已修（`\r?\n`），并补测试。

### 最终验证

- `flutter analyze`：新增文件零问题
- `flutter test`：**15/15 通过**（14 个那年今日 + 1 个 smoke test）
